# Шаблон для добавления нового курса

Этот документ описывает, как использовать `course-template.json` для создания нового курса на сайте.

## Формат данных

Файл содержит массив занятий курса в **правильном формате**, который можно импортировать в Firestore **без конвертации**.

### Ключевые поля каждого занятия:

```json
{
  "period": "general-1",              // ID занятия (уникальный!)
  "title": "Занятие 1: Название",    // Заголовок
  "subtitle": "Описание темы",        // Подзаголовок
  "published": true,                  // Опубликовано ли занятие
  "order": 1,                         // Порядок отображения
  "placeholderEnabled": false,        // Показывать заглушку вместо контента
  "placeholderText": "",              // Текст заглушки (если нужен)
  "sections": { ... }                 // Контент занятия
}
```

### Структура sections:

#### 1. **video_section** — видео-лекции
```json
"video_section": {
  "title": "Видео-лекция",
  "content": [
    {
      "title": "Название видео",
      "url": "https://youtube.com/watch?v=XXXXX",
      "deckUrl": "https://link-to-presentation.pdf",  // опционально
      "audioUrl": "https://link-to-audio.mp3"         // опционально
    }
  ]
}
```

#### 2. **concepts** — понятия
```json
"concepts": {
  "title": "Понятия",
  "content": [
    "Понятие 1",
    "Понятие 2",
    "Понятие 3"
  ]
}
```

#### 3. **authors** — ключевые авторы
```json
"authors": {
  "title": "Ключевые авторы",
  "content": [
    {
      "name": "Имя Автора",
      "url": "https://ru.wikipedia.org/wiki/..."  // опционально
    }
  ]
}
```

#### 4. **core_literature** — основная литература
```json
"core_literature": {
  "title": "Основная литература",
  "content": [
    {
      "title": "Название книги",
      "url": "https://..."  // опционально
    }
  ]
}
```

#### 5. **extra_literature** — дополнительная литература
```json
"extra_literature": {
  "title": "Дополнительная литература",
  "content": [
    {
      "title": "Название книги",
      "url": "https://..."  // опционально
    }
  ]
}
```

## Как добавить новый курс

### Шаг 1: Заполнить course-template.json

1. Скопируй `course-template.json` и переименуй (например, `general-psychology-course.json`)
2. Измени `period` для каждого занятия (например, `general-1`, `general-2`, ...)
3. Заполни `title` и `subtitle` для каждого занятия
4. Добавь контент в `sections`:
   - Видео в `video_section.content`
   - Понятия в `concepts.content`
   - Авторов в `authors.content`
   - Литературу в `core_literature.content` и `extra_literature.content`

### Шаг 2: Создать скрипт импорта

Создай файл `scripts/import-general-psychology.cjs`:

```javascript
const admin = require('firebase-admin');
const fs = require('fs');
const path = require('path');

admin.initializeApp({
  projectId: 'psych-dev-site-prod'
});

const db = admin.firestore();

async function importGeneralPsychology() {
  try {
    const dataPath = path.join(__dirname, '../general-psychology-course.json');
    const topics = JSON.parse(fs.readFileSync(dataPath, 'utf8'));

    console.log(`📚 Найдено ${topics.length} тем для импорта`);

    const batch = db.batch();
    let count = 0;

    for (const topic of topics) {
      // ВАЖНО: измени название коллекции на свою!
      const docRef = db.collection('general-psychology').doc(topic.period);

      const docData = {
        ...topic,
        updatedAt: admin.firestore.FieldValue.serverTimestamp(),
        createdAt: admin.firestore.FieldValue.serverTimestamp()
      };

      batch.set(docRef, docData);
      count++;
      console.log(`  ✓ ${topic.period}: ${topic.title}`);
    }

    await batch.commit();

    console.log(`\n✅ Успешно импортировано ${count} тем в коллекцию 'general-psychology'`);
    console.log('\n📋 Проверьте данные в Firestore Console:');
    console.log('https://console.firebase.google.com/project/psych-dev-site-prod/firestore/data/general-psychology');

  } catch (error) {
    console.error('❌ Ошибка при импорте:', error);
    process.exit(1);
  } finally {
    await admin.app().delete();
  }
}

importGeneralPsychology()
  .then(() => {
    console.log('\n🎉 Импорт завершён');
    process.exit(0);
  })
  .catch((err) => {
    console.error('❌ Критическая ошибка:', err);
    process.exit(1);
  });
```

### Шаг 3: Запустить импорт

```bash
node scripts/import-general-psychology.cjs
```

## Требования к ID занятий (period)

- **Уникальные**: каждый `period` должен быть уникальным
- **Формат**: `название-курса-номер` (например, `general-1`, `general-intro`)
- **Без пробелов**: используй дефисы `-` вместо пробелов
- **Латиница**: только латинские буквы и цифры

## Дополнительные секции (опционально)

Помимо стандартных секций, можно добавить:

### **extra_videos** — дополнительные видео
```json
"extra_videos": {
  "title": "Дополнительные видео",
  "content": [
    {
      "title": "Название видео",
      "url": "https://youtube.com/watch?v=XXXXX"
    }
  ]
}
```

### **leisure** — досуг/развлечения
```json
"leisure": {
  "title": "Досуг",
  "content": [
    {
      "title": "Название материала",
      "url": "https://...",
      "type": "video",  // video, book, article
      "year": "2024"
    }
  ]
}
```

### **self_questions** — вопросы для саморефлексии
```json
"self_questions": {
  "title": "Вопросы для контакта с собой",
  "content": [
    "Вопрос 1?",
    "Вопрос 2?"
  ]
}
```

## Управление заглушками

Чтобы показать заглушку вместо контента:

```json
{
  "period": "general-5",
  "title": "Занятие 5: В разработке",
  "placeholderEnabled": true,                    // ← включить заглушку
  "placeholderText": "Контент в разработке",    // ← текст заглушки
  "sections": { ... }  // можно оставить пустым или заполнить для редактора
}
```

## Примечания

- **Пустые массивы** (`[]`) — это нормально! Секции с пустыми массивами просто не отобразятся на странице
- **Порядок секций** — секции отображаются в следующем порядке:
  1. Видео-лекция
  2. Понятия
  3. Ключевые авторы
  4. Основная литература
  5. Дополнительная литература
  6. Дополнительные видео
  7. Досуг
  8. Вопросы для саморефлексии

## Пример заполненного занятия

```json
{
  "period": "general-1",
  "title": "Предмет психологии",
  "subtitle": "Введение в предмет и методы психологии",
  "published": true,
  "order": 1,
  "placeholderEnabled": false,
  "sections": {
    "video_section": {
      "title": "Видео-лекция",
      "content": [
        {
          "title": "Предмет психологии — лекция",
          "url": "https://youtube.com/watch?v=abc123",
          "deckUrl": "https://slides.com/presentation.pdf"
        }
      ]
    },
    "concepts": {
      "title": "Понятия",
      "content": [
        "Психология",
        "Предмет психологии",
        "Методы психологии",
        "Интроспекция",
        "Наблюдение"
      ]
    },
    "authors": {
      "title": "Ключевые авторы",
      "content": [
        {
          "name": "Вильгельм Вундт",
          "url": "https://ru.wikipedia.org/wiki/Вундт,_Вильгельм"
        },
        {
          "name": "Уильям Джеймс",
          "url": "https://ru.wikipedia.org/wiki/Джеймс,_Уильям"
        }
      ]
    },
    "core_literature": {
      "title": "Основная литература",
      "content": [
        {
          "title": "Гиппенрейтер Ю.Б. Введение в общую психологию",
          "url": "https://..."
        }
      ]
    },
    "extra_literature": {
      "title": "Дополнительная литература",
      "content": [
        {
          "title": "Джеймс У. Психология",
          "url": "https://..."
        }
      ]
    }
  }
}
```
