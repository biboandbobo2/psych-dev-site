# Обзор планов

| Подсистема | План | Документ | Текущий статус | Следующий шаг | Ключевые артефакты |
|------------|------|----------|----------------|---------------|--------------------|
| Core | Refactoring Plan | `docs/CORE_REFACTORING_PLAN.md` | ✅ Все фазы 1‑6 выполняются, Phase 6 QA описана | Поддерживать QA-лог, фиксировать ручные smoke-прогоны | `Docs`, `SaveNoteAsEventButton`, `Notes` компоненты, `notesExport.ts` |
| Timeline | Refactoring Plan | `docs/TIMELINE_REFACTORING_PLAN.md` | ✅ Фазы 1‑5 завершены, Phase 6 дополнил тесты/релизный блок | Документировать manual smoke и `npm run build` результат | `parseBulkEvents`, `formatEventAsNote`, `useTimeline` тесты |
| Tests | Refactoring Plan | `docs/TESTS_REFACTORING_PLAN.md` | 🟢 Основные задачи (перенос утилит, разбивка тестов) завершены | Поддерживать раздел `TESTS_SYSTEM_GUIDE`, актуализировать `ts-prune` | `src/utils/test*`, `tests`/guides |
| Notes & Exports | Infrastructure | `docs/CORE_REFACTORING_PLAN.md` / `ARCHITECTURE_GUIDELINES.md` | ✅ раздел Notes реорганизован, экспорты вынесены в `notesExport.ts` | Следить за тем, чтобы новые заметки использовали существующие хуки/принципы | Новые компоненты `NotesHeader`, `NoteModal`, `SaveNoteAsEventButton` |
| Архитектура | Guideline | `docs/ARCHITECTURE_GUIDELINES.md`, `docs/ARCHITECTURE_PRINCIPLES.md` | ✅ Есть полноценный гайд с SRP/DRY/композицией и checklist | Напомнить о принципах перед новым таском/рефакторингом (см. `README`) | Содержит чеклист перед коммитом и рекомендации по state/hooks |

## QA/Smoke статус
- `SaveNoteAsEventButton` юнит-тесты (`src/components/__tests__/SaveNoteAsEventButton.test.tsx`) — ✅ `vitest` проходит.
- `useTimeline` юнит-тесты (`src/hooks/__tests__/useTimeline.test.ts`) — ✅ `vitest` проходит.
- `npm run build` — ✅ проходит, одно предупреждение по размеру чанков (см. `docs/REFRACTORING_ARCHIVE.md`).
- Ручные проверки: CRUD заметок, экспорт, добавление события — ещё предстоит логировать заранее (см. `docs/REFRACTORING_ARCHIVE.md`).

## Аудит проекта (краткий)
1. Документ `docs/CORE_REFACTORING_PLAN.md` служит основным roadmap и теперь содержит Phase 6 — стоит регулярно рефрешить, если появляются новые задачи.
2. `docs/TIMELINE_REFACTORING_PLAN.md` также расширен, но нужно синхронизовать с TimelineGuide (чтобы описание UI и документация в одном направлении).
3. Архитектурные принципы закреплены в `docs/ARCHITECTURE_GUIDELINES.md`; стоит упомянуть их в `README` (см. задача ниже).

> Обновляйте этот файл после каждой крупной фазы или аудита, чтобы новые участники сразу понимали статус.
