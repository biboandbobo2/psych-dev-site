# План рефакторинга основной части проекта

## 🔗 Связанные документы

- 📋 **[Главная документация](./README.md)** - навигация по всем документам проекта
- 📐 **[Архитектурные рекомендации](./ARCHITECTURE_GUIDELINES.md)** - правила и best practices
- 🧪 **[План рефакторинга тестов](./TESTS_REFACTORING_PLAN.md)** - рефакторинг системы тестирования
- 📅 **[План рефакторинга таймлайна](./TIMELINE_REFACTORING_PLAN.md)** - рефакторинг системы таймлайна

---

## 📊 Текущее состояние

### Общая статистика

**Всего кода в основной части:** ~5900 строк

**Распределение по категориям:**
- Страницы: **3427 строк** (58%)
- Компоненты: **1070 строк** (18%)
- Хуки: **601 строка** (10%)
- Утилиты: **1208 строк** (20%)
- Библиотека: **364 строки** (6%)
- Данные: **581 строка** (10%)
- Авторизация: **~300 строк** (5%)

### Подсистемы проекта

| Подсистема | Строк кода | Основные компоненты | Состояние |
|------------|-----------|---------------------|-----------|
| **Admin** | ~2542 | AdminContentEdit, AdminImport, AdminUsers | ⚠️ Требует рефакторинга |
| **Notes** | ~840 | Notes.tsx, NoteModal, заметки хуки | 🟡 Средняя сложность |
| **Profile** | ~342 | Profile.tsx | ✅ Хорошо |
| **Import/Export** | ~565 | AdminImport, ExportNotesButton | 🟡 Можно улучшить |
| **Theming** | ~303 | theme.ts, color.ts | ✅ Хорошо |
| **Media** | ~207 | mediaUpload.ts, UploadAsset | ✅ Хорошо |
| **Auth** | ~300 | AuthProvider, RequireAuth | ✅ Хорошо |

---

## 🚨 Критические проблемы

### 1. AdminContentEdit.tsx - МОНОЛИТ (956 строк)

**Проблема:** Гигантский компонент, объединяющий:
- Редактор метаданных контента
- Редактор топиков
- Предпросмотр контента
- Загрузку медиафайлов
- Управление изображениями
- Валидацию

**Цель:** Разбить на модули по 200-300 строк

### 2. Legacy данные (319 строк)

**Файлы к удалению:**
- `authorsTestData.ts` (91 строк)
- `authorsTestLevel2Data.ts` (90 строк)
- `authorsTestLevel3Data.ts` (138 строк)

**Причина:** Устаревшие тестовые данные, не используются в production

### 3. Неправильное размещение файлов (565 строк)

**Файлы в `utils/`, которые должны быть в `tests/`:**
- `testImportExport.ts` (396 строк)
- `testAppearance.ts` (169 строк)

### 4. Средние компоненты на грани (453-437 строк)

**Файлы в "оранжевой зоне":**
- `AdminImport.tsx` (453 строк) - импорт топиков и данных
- `Notes.tsx` (437 строк) - система заметок

---

## 📋 План рефакторинга

### Фаза 1: Быстрые победы (1-2 дня)

**Цель:** Уменьшить кодовую базу на ~900 строк без изменения логики

#### Задачи:

1. **Удалить legacy данные** (~1-2 часа)
   ```bash
   # Проверить, что файлы не импортируются
   grep -r "authorsTestData" src/
   grep -r "authorsTestLevel" src/

   # Удалить файлы
   rm src/data/authorsTestData.ts
   rm src/data/authorsTestLevel2Data.ts
   rm src/data/authorsTestLevel3Data.ts
   ```
   - ✅ Результат: -319 строк

2. **Переместить тестовые утилиты** (~1 час)
   ```bash
   # Создать папку для тестовых утилит
   mkdir -p src/pages/tests/utils

   # Переместить файлы
   mv src/utils/testImportExport.ts src/pages/tests/utils/
   mv src/utils/testAppearance.ts src/pages/tests/utils/
   ```
   - Обновить импорты в Tests.tsx и AgeTests.tsx
   - ✅ Результат: -565 строк из utils/, организация улучшена

3. **Проверить неиспользуемый код** (~2-3 часа)
   ```bash
   # Найти экспорты без импортов
   npx ts-prune
   ```
   - Проверить вручную каждый случай
   - Удалить действительно неиспользуемые функции

**Результат фазы 1:**
- Удалено: ~320-400 строк мертвого кода
- Организация: тестовые утилиты на своем месте
- Проект: ~5500 строк

---

### Фаза 2: AdminContentEdit рефакторинг (3-5 дней)

**Цель:** AdminContentEdit.tsx 956 → ~300 строк

#### Архитектура разбиения:

```
src/pages/admin/
├── AdminContentEdit.tsx (300 строк) - главный контейнер
└── content-editor/
    ├── components/
    │   ├── ContentMetadataForm.tsx (150-200 строк) - форма метаданных
    │   ├── ContentTopicsEditor.tsx (150-200 строк) - редактор топиков
    │   ├── ContentMediaManager.tsx (150-200 строк) - управление медиа
    │   └── ContentPreview.tsx (100-150 строк) - предпросмотр
    ├── hooks/
    │   ├── useContentEditor.ts (100-150 строк) - основная логика
    │   ├── useContentValidation.ts (50-80 строк) - валидация
    │   └── useMediaUpload.ts (80-100 строк) - загрузка медиа
    └── types.ts (50-100 строк) - типы редактора
```

#### Подробный план:

**День 1: Подготовка и анализ**
1. Прочитать AdminContentEdit.tsx полностью
2. Составить карту зависимостей (state, props, callbacks)
3. Выделить независимые блоки UI
4. Создать новую структуру папок

**День 2: Извлечение типов и хуков**
1. Создать `content-editor/types.ts`:
   - ContentEditFormData
   - ContentValidationErrors
   - MediaUploadState

2. Создать `useContentEditor.ts`:
   - Основная логика работы с Firestore
   - Загрузка/сохранение контента
   - Управление состоянием формы

3. Создать `useContentValidation.ts`:
   - Валидация полей формы
   - Проверка уникальности ID
   - Валидация медиа-ссылок

4. Создать `useMediaUpload.ts`:
   - Загрузка изображений
   - Обработка ошибок
   - Прогресс загрузки

**День 3: Извлечение форм и превью**
1. Создать `ContentMetadataForm.tsx`:
   - Поля: title, author, year, book
   - Селекторы периодов и топиков
   - Inline валидация

2. Создать `ContentPreview.tsx`:
   - Превью текущего контента
   - Отображение изображений
   - Форматирование текста

**День 4: Медиа и топики**
1. Создать `ContentMediaManager.tsx`:
   - Загрузка изображений
   - Список загруженных файлов
   - Удаление медиа
   - Drag & drop (если есть)

2. Создать `ContentTopicsEditor.tsx`:
   - Редактор топиков
   - Добавление/удаление
   - Валидация структуры

**День 5: Интеграция и тестирование**
1. Обновить `AdminContentEdit.tsx`:
   - Импортировать новые компоненты
   - Использовать новые хуки
   - Минимальная координирующая логика

2. Тестирование:
   - Создание нового контента
   - Редактирование существующего
   - Загрузка медиа
   - Сохранение изменений
   - Проверка валидации

**Чеклист тестирования:**
- [ ] Создание нового контента с нуля
- [ ] Редактирование существующего контента
- [ ] Загрузка одного изображения
- [ ] Загрузка нескольких изображений
- [ ] Удаление изображения
- [ ] Добавление топиков
- [ ] Удаление топиков
- [ ] Валидация пустых полей
- [ ] Валидация дублирующихся ID
- [ ] Сохранение изменений
- [ ] Отмена изменений
- [ ] Предпросмотр контента

**Результат фазы 2:**
- AdminContentEdit.tsx: 956 → ~300 строк
- Создано: 4 компонента + 3 хука + types
- Проект: ~5800 строк (рост за счет структуры, но намного читабельнее)

---

### Фаза 3: AdminImport оптимизация (2-3 дня)

**Цель:** AdminImport.tsx 453 → ~250 строк

#### Архитектура разбиения:

```
src/pages/admin/
├── AdminImport.tsx (250 строк) - главный контейнер
└── import/
    ├── components/
    │   ├── TopicsImportForm.tsx (100-150 строк) - импорт топиков
    │   ├── DataImportForm.tsx (100-150 строк) - импорт данных
    │   └── ImportProgress.tsx (80-100 строк) - прогресс импорта
    ├── hooks/
    │   ├── useTopicsImport.ts (80-120 строк) - логика импорта топиков
    │   └── useDataImport.ts (80-120 строк) - логика импорта данных
    └── types.ts (30-50 строк) - типы импорта
```

#### План:

**День 1: Анализ и подготовка**
1. Проанализировать AdminImport.tsx
2. Выделить два независимых потока: топики и данные
3. Создать структуру папок

**День 2: Извлечение логики**
1. Создать `useTopicsImport.ts`:
   - Парсинг CSV с топиками
   - Загрузка в Firestore
   - Обработка ошибок

2. Создать `useDataImport.ts`:
   - Парсинг различных форматов
   - Валидация данных
   - Batch операции

**День 3: UI компоненты**
1. Создать `TopicsImportForm.tsx`:
   - Выбор файла
   - Предпросмотр данных
   - Кнопка импорта

2. Создать `DataImportForm.tsx`:
   - Аналогично для данных

3. Создать `ImportProgress.tsx`:
   - Прогресс-бар
   - Лог операций
   - Ошибки

4. Обновить главный компонент

**Чеклист тестирования:**
- [ ] Импорт топиков из CSV
- [ ] Импорт данных из JSON
- [ ] Обработка некорректных файлов
- [ ] Предпросмотр перед импортом
- [ ] Отображение прогресса
- [ ] Обработка ошибок при загрузке
- [ ] Откат при ошибке (если реализовано)

**Результат фазы 3:**
- AdminImport.tsx: 453 → ~250 строк
- Создано: 3 компонента + 2 хука
- Проект: ~6000 строк

---

### Фаза 4: Notes система (2-3 дня)

**Цель:** Notes.tsx 437 → ~300 строк

#### Архитектура разбиения:

```
src/pages/notes/
├── Notes.tsx (300 строк) - главный контейнер
└── components/
    ├── NotesHeader.tsx (80-100 строк) - заголовок и фильтры
    ├── NotesList.tsx (100-150 строк) - список заметок
    ├── NotesEditor.tsx (150-200 строк) - редактор заметки
    └── NotesEmpty.tsx (30-50 строк) - пустое состояние
```

**Примечание:** useNotes.ts (212 строк) уже вынесен в хук - это хорошо!

#### План:

**День 1: Анализ**
1. Проанализировать Notes.tsx
2. Выделить независимые UI блоки
3. Проверить зависимости от useNotes

**День 2: Извлечение компонентов**
1. Создать `NotesHeader.tsx`:
   - Заголовок страницы
   - Интеграция с NotesFilter
   - Кнопка создания заметки

2. Создать `NotesEmpty.tsx`:
   - Состояние "нет заметок"
   - Подсказки для пользователя

**День 3: Список и редактор**
1. Создать `NotesList.tsx`:
   - Рендер списка заметок
   - Карточки заметок
   - Действия (редактировать, удалить)

2. Создать `NotesEditor.tsx`:
   - Форма редактирования
   - Селектор топиков
   - Кнопки сохранения/отмены

3. Обновить главный компонент

**Чеклист тестирования:**
- [ ] Отображение списка заметок
- [ ] Фильтрация по топикам
- [ ] Фильтрация по датам
- [ ] Создание новой заметки
- [ ] Редактирование заметки
- [ ] Удаление заметки
- [ ] Сохранение как событие (интеграция с таймлайном)
- [ ] Экспорт заметок

**Результат фазы 4:**
- Notes.tsx: 437 → ~300 строк
- Создано: 4 компонента
- Проект: ~6200 строк

---

### Фаза 5: Мелкие улучшения (1-2 дня)

**Цель:** Оптимизировать компоненты в "желтой зоне" (300-500 строк)

#### Задачи:

1. **Profile.tsx (342 строки) - анализ**
   - Проверить, можно ли извлечь формы
   - Возможно создать ProfileEditor и ProfileView
   - Оценить целесообразность (может быть уже достаточно хорошо)

2. **Хуки - анализ**
   - useNotes.ts (212 строк) - проверить, можно ли разбить
   - useTopics.ts (123 строки) - вероятно, уже хорошо

3. **Компоненты средней сложности:**
   - SaveNoteAsEventButton.tsx (191 строк) - возможно извлечь модальное окно
   - NoteModal.tsx (191 строк) - проверить дублирование с NotesEditor
   - ExportNotesButton.tsx (161 строк) - возможно извлечь логику экспорта

#### План:

**День 1: Анализ и приоритеты**
1. Проанализировать каждый файл
2. Оценить ROI рефакторинга
3. Выбрать 2-3 наиболее критичных

**День 2: Выборочный рефакторинг**
1. Рефакторить выбранные компоненты
2. Тестирование
3. Документация изменений

**Результат фазы 5:**
- Оптимизировано: 2-3 компонента
- Общее улучшение читабельности

---

## 📈 Ожидаемые результаты

### До рефакторинга:
- **Всего:** ~5900 строк
- **Монолитов (>800 строк):** 1 (AdminContentEdit)
- **"Оранжевых" (500-800):** 0
- **"Желтых" (300-500):** 3 (AdminImport, Notes, Profile)
- **"Зеленых" (<300):** остальные

### После рефакторинга:
- **Всего:** ~6000-6500 строк (рост за счет структуры)
- **Монолитов:** 0
- **"Оранжевых":** 0
- **"Желтых":** 0-2 (Profile может остаться)
- **"Зеленых":** все основные компоненты

### Качественные улучшения:
- ✅ Все страницы <300 строк
- ✅ Модульная структура админки
- ✅ Переиспользуемые компоненты форм
- ✅ Независимые хуки для логики
- ✅ Улучшенная тестируемость
- ✅ Удален legacy код
- ✅ Правильная организация файлов

---

## 🔄 Координация с другими рефакторингами

### Связь с рефакторингом тестов

**Точки интеграции:**
- Использование общих хуков (useDataSource)
- Импорт тестовых данных (AdminImport)
- Общие типы (AgeRange в Notes и Tests)

**Рекомендации:**
1. Сначала завершить перемещение testImportExport.ts и testAppearance.ts (Фаза 1)
2. Потом начинать рефакторинг Tests.tsx из TESTS_REFACTORING_PLAN
3. Избегать одновременного изменения общих хуков

**Конфликты:** ❌ Нет критических конфликтов

### Связь с рефакторингом таймлайна

**Точки интеграции:**
- SaveNoteAsEventButton и SaveEventAsNoteButton
- Общие типы (AgeRange, Period)
- useNotes и timeline data

**Рекомендации:**
1. Можно работать параллельно
2. Согласовать изменения в общих типах (types/notes.ts)
3. При рефакторинге Notes.tsx учитывать интеграцию с таймлайном

**Конфликты:** ❌ Нет критических конфликтов

### Порядок выполнения

**Рекомендуемая последовательность:**

1. **Core Фаза 1** (быстрые победы) - 1-2 дня
   - Не влияет на другие рефакторинги

2. **Параллельно:**
   - **Tests Фаза 1-2** (извлечение типов и хуков)
   - **Timeline Фаза 1** (split exporters)

3. **Core Фаза 2** (AdminContentEdit) - 3-5 дней
   - Не зависит от тестов/таймлайна

4. **Параллельно:**
   - **Tests Фаза 3** (разбиение Tests.tsx)
   - **Timeline Фаза 2** (разбиение Timeline.tsx)

5. **Core Фаза 3-4** (AdminImport, Notes) - 4-6 дней
   - Координация с Timeline (SaveNoteAsEventButton)

6. **Фаза 5** всех проектов (мелкие улучшения)
   - Финальные штрихи

**Общее время:** 3-4 недели при последовательном выполнении, 2-3 недели при параллельном

---

## ⚠️ Меры предосторожности

### 1. Backup стратегия

**Перед каждой фазой:**
```bash
# Создать ветку для фазы
git checkout -b refactor/core-phase-X

# После завершения фазы
git add .
git commit -m "refactor(core): Phase X - [описание]"
git push origin refactor/core-phase-X
```

### 2. Тестирование после каждой фазы

**Обязательные проверки:**
1. `npm run dev` - запускается без ошибок
2. `npm run build` - сборка успешна
3. Основные user flows:
   - Вход в админку
   - Создание/редактирование контента
   - Создание/редактирование заметки
   - Импорт данных
   - Загрузка медиа

### 3. Rollback процедура

**Если что-то сломалось:**
```bash
# Вернуться к предыдущему коммиту
git reset --hard HEAD^

# Или к конкретному коммиту
git reset --hard <commit-hash>

# Или создать revert commit
git revert HEAD
```

### 4. Production deploy

**После завершения всех фаз:**
1. Полное тестирование на staging
2. Code review (если есть команда)
3. Создать release ветку
4. Deploy в production
5. Мониторинг ошибок первые 24-48 часов

---

## 📋 Итоговый чеклист

### Перед началом:
- [ ] Прочитать ARCHITECTURE_GUIDELINES.md
- [ ] Создать ветку refactor/core
- [ ] Убедиться, что текущая версия стабильна
- [ ] Сделать backup БД Firebase (если критичные данные)

### Фаза 1 (Быстрые победы):
- [ ] Удалить authorsTest*Data.ts файлы
- [ ] Переместить testImportExport.ts и testAppearance.ts
- [ ] Обновить импорты
- [ ] Запустить `npx ts-prune` и удалить мертвый код
- [ ] Протестировать основные flows
- [ ] Commit & push

### Фаза 2 (AdminContentEdit):
- [ ] Создать структуру папок content-editor/
- [ ] Создать types.ts
- [ ] Создать useContentEditor.ts
- [ ] Создать useContentValidation.ts
- [ ] Создать useMediaUpload.ts
- [ ] Создать ContentMetadataForm.tsx
- [ ] Создать ContentPreview.tsx
- [ ] Создать ContentMediaManager.tsx
- [ ] Создать ContentTopicsEditor.tsx
- [ ] Обновить AdminContentEdit.tsx
- [ ] Полное тестирование (см. чеклист выше)
- [ ] Commit & push

### Фаза 3 (AdminImport):
- [ ] Создать структуру папок import/
- [ ] Создать types.ts
- [ ] Создать useTopicsImport.ts
- [ ] Создать useDataImport.ts
- [ ] Создать TopicsImportForm.tsx
- [ ] Создать DataImportForm.tsx
- [ ] Создать ImportProgress.tsx
- [ ] Обновить AdminImport.tsx
- [ ] Тестирование импорта
- [ ] Commit & push

### Фаза 4 (Notes):
- [ ] Создать структуру папок notes/components/
- [ ] Создать NotesHeader.tsx
- [ ] Создать NotesEmpty.tsx
- [ ] Создать NotesList.tsx
- [ ] Создать NotesEditor.tsx
- [ ] Обновить Notes.tsx
- [ ] Тестирование CRUD заметок
- [ ] Commit & push

### Фаза 5 (Финал):
- [ ] Проанализировать оставшиеся компоненты
- [ ] Выборочный рефакторинг 2-3 файлов
- [ ] Финальное тестирование всего проекта
- [ ] Обновить документацию
- [ ] Commit & push

### После всех фаз:
- [ ] Merge в main ветку
- [ ] Deploy на staging
- [ ] QA тестирование
- [ ] Deploy на production
- [ ] Мониторинг 24-48 часов

---

## 📚 Дополнительные ресурсы

- [React Component Patterns](https://reactpatterns.com/)
- [Refactoring UI](https://refactoringui.com/)
- [Clean Code principles](https://github.com/ryanmcdermott/clean-code-javascript)

---

**Версия документа:** 1.0
**Дата создания:** 2025-11-07
**Автор:** Claude Code Agent
**Статус:** 📋 План готов к выполнению
