# Timeline Guide

Подробное описание текущей реализации страницы `src/pages/Timeline.tsx` и связанных модулей.

## 🔗 Связанные документы

- 📋 **[Главная документация](../README.md)** - навигация по всем документам проекта
- 📐 **[Архитектурные рекомендации](../architecture/guidelines.md)** - правила и best practices
- 🔧 **[Архив рефакторингов](../archive/REFRACTORING_ARCHIVE.md#timeline)** - завершённые этапы и контекст по Timeline
- 🗂️ **[Актуальный бэклог](../processes/audit-backlog.md)** - текущие задачи и техдолг по системе
- 📚 **[Система тестирования](./testing-system.md)** - связанная система (интеграция через заметки и возрастные периоды)

---

## Обзор

- Доступна авторизованным пользователям по маршруту `/timeline`.
- Рендерится как полноэкранный `SVG`‑холст с панелями управления слева и справа.
- Основная цель — визуализировать жизненный путь пользователя, фиксировать события, личные решения и альтернативные ветки развития.

## Структура кода

Код таймлайна организован в модули для удобства поддержки:

```
src/pages/
├── Timeline.tsx                  # Основной компонент (~503 строк)
└── timeline/                     # Модули таймлайна
    ├── index.ts                  # Экспорты всех модулей
    ├── types.ts                  # TypeScript типы и интерфейсы
    ├── constants.ts              # Константы (цвета, размеры, метаданные)
    ├── utils/
    │   ├── index.ts              # Основные утилиты (координаты, парсинг)
    │   ├── ageToRange.ts         # Преобразование возраста в AgeRange
    │   └── exporters.ts          # Экспорт в JSON, PNG, PDF
    ├── components/
    │   ├── IconPickerButton.tsx         # Компонент выбора пиктограмм
    │   ├── PeriodizationSelector.tsx    # Селектор периодизации развития
    │   ├── PeriodizationLayer.tsx       # Визуализация периодов на холсте
    │   ├── PeriodBoundaryModal.tsx      # Модальное окно границы между периодами
    │   ├── BulkEventCreator.tsx         # Массовое создание событий
    │   └── SaveEventAsNoteButton.tsx    # Сохранение события как заметки
    ├── hooks/
    │   └── useTimelineHistory.ts # Хук для undo/redo функциональности
    └── data/
        └── periodizations.ts     # Данные периодизаций от психологов

src/components/
├── NoteModal.tsx                 # Модальное окно редактирования заметки
└── SaveNoteAsEventButton.tsx     # Сохранение заметки как события

src/hooks/
├── useNotes.ts                   # Работа с заметками (CRUD операции)
└── useTimeline.ts                # Добавление событий на таймлайн из заметок

## Feature-based структура и экспортные точки

- `src/pages/timeline/index.ts` выступает как баррель: экспортирует типы, константы, утилиты в `timeline/utils`, компоненты (`IconPickerButton`, `PeriodizationSelector`, `PeriodizationLayer`, `PeriodBoundaryModal`), хук `useTimelineHistory` и статические данные `data/periodizations`. Это упрощает импорты в `src/pages/Timeline.tsx` и других частях приложения.
- Каждый компонент/хук живёт рядом со своей функциональной зоной:
  - Компоненты (`components/…`) управляют UI-интерфейсами (`IconPickerButton`, `PeriodizationSelector`, панель границ `PeriodBoundaryModal`).
  - `hooks/useTimelineHistory` держит историю undo/redo и используется совместно с `useTimelineState`.
  - `utils/exporters.ts` и `utils/index.ts` — повторно используемые утилиты (экспорт данных, координаты, парсинг возраста).
  - `data/periodizations.ts` содержит все актуальные периодизации и их метаданные, поэтому импортируется в форму выбора периодизации и TimelineCanvas.
- `src/pages/Timeline.tsx` остаётся точкой интеграции: он собирает состояния из `useTimelineState`, рендерит `TimelineCanvas` и панели, экспортирует данные в `SaveNoteAsEventButton` и `NoteModal`, и подключает `src/pages/timeline/index.ts` для любых reusable элементов.
- Согласно архивной версии `docs/archive/legacy/TIMELINE_REFACTORING_PLAN.md`, текущие задачи по таймлайну переехали в `docs/processes/audit-backlog.md` и `docs/archive/REFRACTORING_ARCHIVE.md` (секция Timeline). При работе с новым функционалом ориентируйтесь на эти документы: гайды синхронизированы, а этот файл описывает реализацию и любые отклонения от плана.

## Новая структура после рефакторинга

- **`TimelineCanvas.tsx`** — теперь весь `SVG`‑рендер и обработка мыши/колёсика вынесены сюда; Canvas получает трансформацию, события, ветки и callback’и (`onSelectBranch`, `onSelectBirth`, `onPeriodBoundaryClick`).
- **`TimelineLeftPanel.tsx`** и **`TimelineRightPanel.tsx`** собирают UI управления (возраст, прокрутка, экспорт, формы) и передают действия обратно в `Timeline.tsx`.
- **Формы** (`TimelineBirthForm`, `TimelineEventForm`, `TimelineBranchContinuation`, `TimelineBranchEditor`) теперь компоненты с минимальной логикой, они получают только пропсы и используют shared UI (IconPicker, сохранение заметки).
- **Утилиты**: `parseBulkEvents` и `formatEventAsNote` находятся в `src/pages/timeline/utils/`; они покрыты Vitest и используются в `BulkEventCreator` и `SaveEventAsNoteButton`.
- **`BulkEventCreator.tsx`** продолжает жить в `timeline/components` и теперь вызывает `parseBulkEvents`, которые возвращают структурированные строчки и статус расширения ветки.
```

### Модули

**types.ts** - Все TypeScript типы:
- `Sphere` - сферы жизни (образование, карьера, семья и т.д.)
- `NodeT` - событие на таймлайне
- `EdgeT` - ветка (альтернативная линия жизни)
- `BirthDetails` - детали рождения
- `TimelineData` - полные данные таймлайна (включая `selectedPeriodization`)
- `HistoryState` - состояние для undo/redo
- `SaveStatus` - статус сохранения
- `Transform` - трансформация SVG-холста
- `Period` - период в периодизации (возрастной диапазон, описание, цвет)
- `Periodization` - периодизация развития от психолога (критерий, список периодов, гибкость границ)

**constants.ts** - Константы конфигурации:
- `YEAR_PX` - пиксели на год (80px)
- `LINE_X_POSITION` - X-координата основной линии (2000px)
- `NODE_RADIUS`, `MIN_SCALE`, `MAX_SCALE` - параметры отображения
- `SPHERE_META` - метаданные сфер (цвета, названия, эмодзи)
- `SAVE_DEBOUNCE_MS` - задержка автосохранения (10000ms)

**utils/** - Вспомогательные функции:
- `utils/index.ts`:
  - `screenToWorld()` - преобразование экранных координат в координаты мира
  - `clamp()` - ограничение значения в пределах min-max
  - `parseAge()` - парсинг возраста из строки
  - `removeUndefined()` - удаление undefined значений для Firestore
- `utils/ageToRange.ts`:
  - `ageToRange()` - преобразование числового возраста в категориальный AgeRange
  - При граничных значениях выбирается старший период (например, 7 лет → 'primary-school', а не 'preschool')
- `utils/exporters.ts`:
  - `exportTimelineJSON()`, `exportTimelinePNG()`, `exportTimelinePDF()` - экспорт таймлайна в различных форматах

**components/IconPickerButton.tsx** - Компонент выбора пиктограмм:
- Всплывающий селектор из 40 PNG-иконок
- Поддержка двух цветовых тем (emerald/sky)
- Автоматическое закрытие при клике вне компонента

**components/PeriodizationSelector.tsx** - Селектор периодизации развития:
- Компактная кнопка с иконкой учёного (👨‍🏫) и текстом
- Когда выбрана периодизация: отображает фамилию психолога
- Когда не выбрана: отображает текст "Периодизации"
- Всплывающее меню со списком всех доступных периодизаций
- Автоматическое закрытие при клике вне компонента

**components/PeriodizationLayer.tsx** - Визуализация периодов на холсте:
- Рендерит горизонтальные полосы с пастельными цветами для каждого периода
- Пунктирные линии между периодами для обозначения границ
- Кликабельные области на границах (60px высота для удобства)
- Hover-эффект при наведении на границу
- Оптимизированная отрисовка: сначала все прямоугольники, затем все линии (для перекрывающихся периодов)

**components/PeriodBoundaryModal.tsx** - Модальное окно границы между периодами:
- Отображается при клике на границу между периодами
- Показывает информацию о переходном возрасте
- Описание периода "до" и периода "после" перехода
- Критерий деления периодов для данной периодизации
- Специальное предупреждение для периодизаций с гибкими границами (например, Кольберг)
- Ссылка на Wikipedia для подробного изучения
- Закрывается по клику вне окна или по кнопке

**components/BulkEventCreator.tsx** - Массовое создание событий:
- Модальное окно для быстрого создания множества событий
- Формат ввода: "возраст, название события" (каждое событие с новой строки)
- Поддержка создания событий на основной линии и на ветках
- Валидация возрастных границ для веток
- Автоматическое продление ветки, если события выходят за её пределы
- Превью всех событий перед созданием

**components/SaveEventAsNoteButton.tsx** - Сохранение события как заметки:
- Кнопка-иконка (📄) в форме редактирования события
- Модальное окно с превью будущей заметки
- Автоматическое определение возрастного периода через `ageToRange()`
- Передача всех деталей события (возраст, период, сфера, подробности) в заметку
- Использует React Portal для корректного отображения
- Детектор состояния модального окна для отладки (логирование z-index, position, display)

**hooks/useTimelineHistory.ts** - Управление историей:
- `saveToHistory()` - сохранение снимка состояния
- `undo()`, `redo()` - навигация по истории
- `canUndo`, `canRedo` - флаги доступности
- Максимальная глубина 50 шагов

**data/periodizations.ts** - Базовые периодизации развития:
- Массив `PERIODIZATIONS` с данными от 7 психологов и педагогов
- Каждая периодизация содержит: автора, критерий деления, список периодов, ссылку на Wikipedia
- Пастельная цветовая палитра для визуального различения периодов
- Функция `getPeriodizationById()` для поиска периодизации по ID
- Доступные периодизации:
  - **Пиаже** (Жан Пиаже) - созревание интеллекта (4 периода: 0-100 лет)
  - **Выготский** (Лев Выготский) - социальная ситуация развития и ведущая деятельность (6 периодов: 0-21 лет)
  - **Эриксон** (Эрик Эриксон) - психосоциальные конфликты (8 периодов: 0-100 лет)
  - **Фрейд** (Зигмунд Фрейд) - психосексуальное развитие (5 периодов: 0-100 лет)
  - **Монтессори** (Мария Монтессори) - самостоятельное созревание личности (4 периода: 0-24 лет)
  - **Гезелл** (Арнольд Гезелл) - биологическое созревание нервной системы (5 периодов: 0-16 лет)
  - **Кольберг** (Лоуренс Кольберг) - моральное развитие (6 стадий: 4-100 лет, гибкие границы)

**src/components/NoteModal.tsx** - Модальное окно редактирования заметки:
- Форма создания/редактирования заметки с полями: заголовок, содержание, возрастной период, тема
- Интеграция TopicSelector для выбора тем из библиотеки вопросов
- Кнопка "📍 На таймлайн" в footer (слева) для создания события из заметки
- Кнопки "Отмена" и "Сохранить" (справа)
- Использует хук `useTimeline` для доступа к функции `addEventToTimeline`

**src/components/SaveNoteAsEventButton.tsx** - Сохранение заметки как события:
- Кнопка "📍 На таймлайн" с модальным окном формы
- Обязательные поля: возраст события (0-100 лет), название события
- Название предзаполняется из заголовка заметки
- Содержание заметки переносится в поле "подробности" события
- Создаёт событие на основной линии жизни (LINE_X_POSITION)
- Сфера жизни НЕ устанавливается автоматически
- Использует React Portal для корректного z-index

**src/hooks/useNotes.ts** - Работа с заметками:
- `createNote()` - создание новой заметки в Firestore
- `updateNote()` - обновление существующей заметки
- `deleteNote()` - удаление заметки
- Real-time подписка на изменения заметок пользователя
- Фильтрация по возрастному периоду
- Сортировка по дате обновления

**src/hooks/useTimeline.ts** - Добавление событий на таймлайн:
- `addEventToTimeline()` - создание события на таймлайне из внешних компонентов (например, заметок)
- Загружает текущие данные таймлайна из Firestore
- Генерирует UUID для нового события
- Добавляет событие в массив nodes
- Сохраняет обновлённые данные с serverTimestamp()
- Используется компонентом SaveNoteAsEventButton

## UI-структура

- **Левая панель** (`fixed left`):
  - Кнопка возврата в профиль.
  - Поле текущего возраста — влияет на длину «прожитой» части основной линии.
  - Ползунок «Прокрутка» — вертикально перемещает вьюпорт по возрастам.
  - Вертикальный слайдер масштаба и отображение текущего zoom'а.
  - Блок статистики: количество событий и распределение по сферам.
- **Центральный холст**:
  - Вертикальная линия жизни по центру (`LINE_X_POSITION = 2000`), разделённая на сплошную (прошедшее) и пунктирную (будущее) части.
  - Шкала возраста каждые 5 лет по всей ширине холста.
  - События отображаются как окружности с подписью, цвет зависит от сферы, отметка «решение» рисуется крестиком.
  - Горизонтальные «веточки» соединяют событие с родительской линией (основной или ответвлением).
  - Отдельные вертикальные линии (edges) визуализируют ветки с собственным диапазоном возрастов.
- **Правая панель** (`fixed right`):
  - Заголовок с индикатором статуса сохранения (idle/saving/saved/error) и селектором периодизации.
  - Универсальная форма создания/редактирования события.
  - Информеры о выбранной ветке или основной линии.
  - При выборе ветки показывается редактор длины ветки и кнопка удаления.
  - Блок Undo/Redo с горячими клавишами.
  - Модальное окно с инструкцией (управляется `showHelp`), по умолчанию скрыто — вызов планируется через UI-триггер.

## Модель данных

- **Событие (`NodeT`)**
  - `id`, `age`, `label`, `notes`, `isDecision`.
  - `sphere` опционален, выбран из перечисления `Sphere`.
  - `x` — горизонтальная координата (совпадает с линией, если событие на основной оси).
  - `parentX` — исходная линия, с которой было создано событие (помогает строить горизонталь).
- **Ветка (`EdgeT`)**
  - `id`, `x`, `startAge`, `endAge`, `color`, `nodeId` (событие-источник).
- **TimelineData**
  - `currentAge`, `ageMax` (фиксирован 100), `nodes`, `edges`, `birthDetails`.
  - `selectedPeriodization` — ID выбранной периодизации или `null` (если периодизация не выбрана).
- **Периодизация (`Periodization`)**
  - `id`, `name`, `author`, `wikiUrl`, `criterion` — метаданные периодизации.
  - `periods` — массив периодов с возрастными границами, названиями, описаниями и цветами.
  - `flexibleBoundaries` — флаг нечётких границ (для периодизаций вроде Кольберга).
  - `boundaryNote` — текстовое примечание о гибкости границ, отображается в модальном окне.
- **Период (`Period`)**
  - `startAge`, `endAge` — возрастной диапазон периода (в годах).
  - `name`, `description` — название и описание периода.
  - `color` — пастельный цвет для визуализации на холсте.
- История (`history`, `historyIndex`) хранит последовательные снимки `nodes + edges` с глубиной до 50 шагов.

## Ключевые сценарии

- **Пиктограммы событий**
  - В шапке форм добавлена кнопка выбора иконки; доступен всплывающий селектор из 40 PNG (`public/icons/events`, метаданные `icons.json`). Выбор сохраняется в `iconId` и заменяет стандартный круг на холсте.
  - Пиктограмма масштабируется вместе с событием; базовый круг и крестик скрываются, чтобы не мешать.
  - Для событий без пиктограммы круг остаётся белым с цветной обводкой сферы, крестик «моё решение» отображается поверх.
- **Профиль рождения**
  - Точка «👶 Рождение» на основной линии активна: при клике открывается карточка с полями «Дата рождения», «Город / место» и «Обстоятельства».
  - Данные сохраняются в `birthDetails` и отправляются в Firestore; undo/redo поддерживает изменения.
  - Карточку можно закрыть без сохранения («Закрыть») или очистить значения перед сохранением («Очистить»).
- **Редактирование события**
  - Клик по событию открывает форму с текущими данными и фиксирует исходные значения для проверки изменений.
  - Добавлены кнопка отмены и возможность удаления события (удаляет и связанные ветки).
- **Ветки**
  - Кнопка «Продолжить ветку» в форме события создаёт вертикальный edge заданной длины.
  - В правой панели доступно изменение длины ветки и удаление (события на ней возвращаются к родительской линии).
  - При перетаскивании события обновляются координаты его веток и всех прикреплённых к ним событий.
- **Перемещение и масштаб**
  - Колесо мыши масштабирует сцену с привязкой к главной линии (ограничение `0.2 ≤ k ≤ 3`).
  - Перетаскивание холста мышью; вьюпорт также синхронизируется с ползунком возраста.
  - События перетаскиваются pointer-событиями; по завершении drag состояние сохраняется в историю.
- **Undo/Redo и горячие клавиши**
  - Cmd/Ctrl+Z, Cmd/Ctrl+Shift+Z работают вне текстовых полей.
  - Delete удаляет выбранное событие, Escape снимает выделение.
- **Периодизации развития**
  - Кнопка селектора периодизации размещена в заголовке правой панели рядом со статусом сохранения.
  - При выборе периодизации на холсте отображаются горизонтальные полосы с пастельными цветами, соответствующие возрастным периодам.
  - Границы между периодами обозначены пунктирными линиями (серый цвет, 2px, dash pattern "8 4").
  - При наведении на границу появляется hover-эффект (светло-серая заливка).
  - При клике на границу открывается модальное окно с подробной информацией:
    - Название периодизации и автор
    - Возраст перехода между периодами
    - Описание периода "до" и "после" границы
    - Критерий деления для данной периодизации
    - Для периодизаций с гибкими границами (например, Кольберг) — специальное предупреждение
    - Дополнительная ссылка: для большинства схем ведёт на Wikipedia, а для опции «Как в курсе» — на раздел сайта, посвящённый младшему из двух возрастов
  - Выбор периодизации сохраняется в Firestore вместе с остальными данными таймлайна.
  - Отображение периодов учитывает перекрывающиеся возрастные диапазоны (используется оптимизированный порядок рендеринга).
  - Пользователь может в любой момент отключить периодизацию, выбрав опцию "Без периодизации".
  - Периодизация «Как в курсе» использует те же возрастные блоки, что и образовательный портал: описания границ повторяют программу занятий и позволяют одним кликом перейти к материалам нужного раздела.

## Связь таймлайна и заметок

Таймлайн и система заметок тесно интегрированы, позволяя пользователю переносить информацию между этими двумя инструментами.

### Сохранение события как заметки

- **Триггер**: В форме редактирования события, слева от чекбокса "Это было моё решение", расположена иконка заметки (📄).
- **Компонент**: `SaveEventAsNoteButton` (`src/pages/timeline/components/SaveEventAsNoteButton.tsx`)
- **Поведение**:
  1. При клике открывается модальное окно подтверждения с превью будущей заметки.
  2. Показывается: название события, возраст, возрастной период (определяется автоматически через `ageToRange`), сфера жизни.
  3. После подтверждения создаётся заметка с:
     - **Заголовок**: название события
     - **Содержание**: структурированный текст с возрастом, периодом, сферой жизни и подробностями события
     - **AgeRange**: автоматически определяется по возрасту (при граничных значениях выбирается старший период)
     - **topicId/topicTitle**: `null` (заметка не привязана к теме)
  4. Модальное окно рендерится через **React Portal** в `document.body`, чтобы избежать конфликтов с z-index иерархией таймлайна.

- **Утилита определения периода**: `ageToRange` (`src/pages/timeline/utils/ageToRange.ts`)
  - Преобразует числовой возраст в категориальный `AgeRange` (например, 25 лет → `'earlyAdult'`)
  - **Важно**: При граничных значениях (например, 7 лет на границе `preschool` (3-7) и `primary-school` (7-10)) выбирается старший/более поздний период (`primary-school`)
  - Полностью совпадает с логикой периодизации заметок для консистентности данных

### Сохранение заметки как события

- **Триггер**: В модальном окне редактирования заметки (`NoteModal`), в footer слева от кнопок "Отмена" и "Сохранить", расположена кнопка "📍 На таймлайн".
- **Компонент**: `SaveNoteAsEventButton` (`src/components/SaveNoteAsEventButton.tsx`)
- **Хук**: `useTimeline` (`src/hooks/useTimeline.ts`) предоставляет функцию `addEventToTimeline`
- **Поведение**:
  1. При клике открывается модальное окно с формой:
     - **Возраст события** (обязательное поле, 0-100 лет)
     - **Название события** (обязательное поле, предзаполняется заголовком заметки)
     - Показывается превью будущего события
  2. После подтверждения создаётся событие на таймлайне:
     - **age**: введённый пользователем возраст
     - **label**: название события
     - **notes**: содержание заметки (полный текст)
     - **x**: `LINE_X_POSITION` (событие создаётся на основной линии жизни)
     - **sphere**: `undefined` (сфера жизни НЕ устанавливается автоматически)
     - **isDecision**: `false`
     - **iconId**: `undefined`
  3. Событие добавляется в Firestore в коллекцию `timelines/{userId}` через функцию `addEventToTimeline`:
     - Загружаются текущие данные таймлайна
     - Генерируется UUID для нового события
     - Событие добавляется к существующему массиву `nodes`
     - Обновлённые данные сохраняются с `serverTimestamp()`
  4. Модальное окно рендерится через **React Portal** для корректного z-index
  5. После успешного создания показывается уведомление "Событие добавлено на таймлайн!"

### Архитектурные особенности

- **React Portal**: Оба модальных окна используют `createPortal(modalContent, document.body)` для рендеринга вне иерархии родительских компонентов. Это решает проблемы с CSS stacking context и гарантирует, что модальные окна всегда отображаются поверх всего контента (z-index: 9999).
- **Детектор состояния** (только в `SaveEventAsNoteButton`): Компонент содержит `useEffect` с детальным логированием состояния модального окна для отладки проблем с видимостью:
  ```typescript
  useEffect(() => {
    if (showConfirm) {
      console.log('✅ [DETECTOR] Modal window is NOW OPEN');
      console.log('✅ [DETECTOR] Z-index should be: z-[9999]');
      // ... проверка DOM и computed styles
    }
  }, [showConfirm]);
  ```
- **Асинхронность**: Обе операции создания (заметки/события) выполняются асинхронно через Promise с обработкой ошибок и индикацией загрузки (disabled-состояние кнопок, текст "Сохранение..." / "Создание...").
- **Валидация**: Проверка обязательных полей (возраст, название) выполняется на клиенте перед отправкой в Firestore.

### Типы и интерфейсы

- **NodeT** (`src/pages/timeline/types.ts`) — событие на таймлайне
- **AgeRange** (`src/types/notes.ts`) — категориальный возрастной период
- **AGE_RANGE_LABELS** (`src/types/notes.ts`) — человекочитаемые названия периодов
- **Sphere** (`src/pages/timeline/types.ts`) — сферы жизни (education, career, family и т.д.)

### Workflow пользователя

**Событие → Заметка**:
1. Пользователь создаёт событие на таймлайне (например, "Поступил в университет", 18 лет, сфера "Образование")
2. Кликает на иконку заметки в форме события
3. Видит превью с автоматически заполненными полями
4. Подтверждает создание
5. В разделе "Заметки" появляется новая заметка с названием "Поступил в университет" и содержанием:
   ```
   **Возраст:** 18 лет
   **Период:** Подростковость (14-18 лет)
   **Сфера жизни:** Образование
   **Подробности:**
   [текст из поля "подробности" события]
   ```

**Заметка → Событие**:
1. Пользователь пишет заметку с размышлениями о важном моменте жизни
2. Кликает "📍 На таймлайн" в footer модального окна
3. Вводит возраст (например, 22 года) и при необходимости редактирует название
4. Подтверждает создание
5. На таймлайне появляется новое событие на основной линии жизни с полным текстом заметки в поле "подробности"
6. Пользователь может затем отредактировать событие: добавить сферу жизни, пиктограмму, создать ветку развития и т.д.

## Сохранение и загрузка

- Автосохранение (`setTimeout` на 10 секунд) срабатывает при наличии событий или веток.
- Перед отправкой в Firestore данные проходят через `removeUndefined`.
- Документ сохраняется в коллекции `timelines/{userId}` с `updatedAt`.
- При загрузке:
  - Восстанавливаются события и ветки, нормализуются `x`/`parentX`.

## Экспорт таймлайна

- В шапке правой панели, справа от кнопки `Выход`, добавлена иконка «📥» для выгрузки данных. При клике появляется компактное меню с вариантами `JSON`, `PNG` и `PDF`.
- `JSON` формируется из текущего `TimelineExportPayload` (узлы, ветки, детали рождения, периодизация) и сохраняется локально на устройстве пользователя. Формат соответствует ожидаемым данным редактора, поэтому файл можно использовать для импорта или бэкапа.
- `PNG` и `PDF` рендерятся через единый пайплайн `src/pages/timeline/utils/exporters.ts`: SVG-контент сериализуется с дополнительными отступами, изображения пиктограмм инлайнатся в DataURL, после чего результат преобразуется в канвас. Это позволяет корректно отображать выбранные пользователем иконки и цветовую схему.
- Для PDF используется A4-страница в портретной ориентации. Изображение масштабируется пропорционально и центрируется, а увеличенный нижний отступ гарантирует попадание подписи «Рождение» и других элементов в итоговый документ.
- Экспорт не требует дополнительных настроек, поэтому юзер может сохранять полотно в любом состоянии — учитывается текущий масштаб, выбранная периодизация и все ветки событий.
  - Вьюпорт центрируется по текущему возрасту (или по рождению, если данных нет).

## Стили и визуал

- Сферы жизни используют пастельную палитру, совпадающую с `SPHERE_META`.
- Подписи и большинство текстов в панели — шрифт `Georgia, serif`.
- Холст масштабирован на ширину `4000px`, что оставляет пространство для боковых веток.
- Индикатор сохранения и панели оформлены градиентными фонами и полупрозрачными блоками.

## Известные проблемы и решения

### Проблема: события не двигаются вместе с родительской веткой

**Симптомы**: При перетаскивании события, от которого идёт ветка, дочерние события на этой ветке «зависают в воздухе» и не следуют за родителем.

**Система координат**:
- `node.x` — текущая X-координата события
- `node.parentX` — X-координата родительской линии (обновляется при движении родителя)
- `edge.x` — X-координата вертикальной линии ветки
- Y-координата абсолютна и определяется возрастом события

**Причина бага**: В функции `handleNodeDragMove` (Timeline.tsx:673) использовалась координата начала перетаскивания (`dragStartNodeX`) вместо текущей координаты события:

```typescript
// НЕПРАВИЛЬНО:
const oldX = dragStartNodeX; // Координата в начале перетаскивания

// ПРАВИЛЬНО:
const oldX = node.x ?? LINE_X_POSITION; // Текущая координата события
```

При использовании стартовой координаты:
1. Первое движение мыши: находит детей с `parentX === 2100`, обновляет их на `parentX === 2110`
2. Второе движение мыши: ищет детей с `parentX === 2100`, не находит никого (они уже 2110)
3. Дети перестают обновляться

**Решение**: Всегда использовать **текущую** координату события (`node.x`) в рекурсивной функции обновления. Это гарантирует, что на каждом шаге перетаскивания система найдёт всех актуальных потомков.

**Рекурсивное обновление** (Timeline.tsx:676-726):
- Находит все события с `parentX === fromX`
- Обновляет их `parentX` на `toX`
- Для событий **на линии** (`node.x === fromX`): перемещает на `toX`
- Для событий **со смещением** (`node.x !== fromX`): сохраняет смещение через `deltaX`
- Рекурсивно обрабатывает ветки от этих событий

### Проблема: перекрывающиеся периоды скрывают границы

**Симптомы**: У некоторых периодизаций (например, Кольберг) границы между периодами не видны и не кликабельны, хотя периоды корректно отображаются.

**Причина**: Периоды с перекрывающимися возрастными диапазонами (стадия 3: 9-13 лет, стадия 4: 13-17 лет, стадия 5: 16-35 лет) рендерились в одном цикле map(). При отрисовке прямоугольник следующего периода перекрывал линию границы предыдущего.

**Решение**: Разделение рендеринга на два этапа в `PeriodizationLayer.tsx`:

```typescript
// Этап 1: Все прямоугольники периодов
{periodization.periods.map((period, index) => (
  <rect key={`period-rect-${index}`} {...} />
))}

// Этап 2: Все линии границ (поверх прямоугольников)
{periodization.periods.map((period, index) => (
  <g key={`period-boundary-${index}`}>
    <line {...} />
    <rect {...} /> {/* Кликабельная область */}
  </g>
))}
```

Такой порядок гарантирует, что все линии границ рисуются после всех прямоугольников, оставаясь видимыми и интерактивными даже при перекрытии периодов.

### Проблема: координаты границ периодов

**Система координат периодов**: Холст таймлайна растёт снизу вверх (рождение внизу, будущее вверх), что требует инверсии Y-координат.

**Расчёт позиций** (PeriodizationLayer.tsx:30-33):
```typescript
const startY = worldHeight - Math.min(period.endAge, ageMax) * YEAR_PX;
const endY = worldHeight - period.startAge * YEAR_PX;
const height = endY - startY;
```

- `startY` — верхняя граница прямоугольника (старший возраст периода)
- `endY` — нижняя граница прямоугольника (младший возраст периода)
- Линия границы рисуется на `startY` (между текущим и следующим периодом)

**Важно**: Использовать `startY` для линий границ, а не `endY`, чтобы линия рисовалась между периодами, а не на рождении или вне возрастного диапазона.

## Риски и планы на развитие

- UI зависит от полноэкранного рендера; при встраивании в другую оболочку потребуется адаптация `transform`.
- Нет явного триггера для открытия модального окна помощи — стоит добавить кнопку/клавишу.
- Потребуется отдельная документация по миграции старых данных (без `edges`/`parentX`) при развёртывании.
- **Периодизации**:
  - Текущая реализация использует эмодзи-иконку (👨‍🏫) для кнопки селектора. Планируется замена на PNG-иконку 20×20px для консистентности с остальным UI.
  - База данных периодизаций захардкожена в `data/periodizations.ts`. При необходимости можно вынести в Firestore для динамического управления.
  - Возможное расширение: добавление пользовательских периодизаций, сравнение нескольких периодизаций на одном холсте.
- Возможное расширение: экспорт таймлайна, фильтрация по сферам, история ветвлений во времени.

## Phase 6 QA и метрики (Timeline)

- Чеклист Phase 6 живёт в `docs/processes/audit-backlog.md` и `docs/archive/REFRACTORING_ARCHIVE.md` (Timeline section). Перед финальной сборкой:
  1. Прогоняйте `npm run test`, `npm run build`, ручные smoke-сценарии (CRUD заметок, экспорт) и фиксируйте результаты в `docs/processes/qa-smoke-log.md`.
  2. Проверяйте, что Timeline отражает те же механизмы, что описаны в `docs/archive/legacy/TIMELINE_REFACTORING_PLAN.md` и `docs/archive/REFRACTORING_ARCHIVE.md` — архитектурные решения и новые компоненты должны быть синхронизированы.
  3. Отмечайте любые отклонения (баги, регрессии) и привязывайте их к тикетам/коммитам, чтобы QA видела полный контекст изменений.
  4. Автоматизируйте Lighthouse для таймлайна через `npm run lighthouse:timeline` (предварительно запуская `npm run dev -- --port=4173` или `npm run preview`), чтобы отчёты попадали в `logs/lighthouse-timeline.json`.

### Замеры сборки (`npm run build`)

- Команда `npm run build` выполнена на Vite 7.1.7; сборка прошла, но Vite сигнализирует о чанках > 500 кБ (предупреждение оставлено в логах).
  - `dist/index.html` — 0.62 кБ (gzip: 0.37 кБ)
  - `dist/assets/index-BsbPjJZN.css` — 69.85 кБ (gzip: 14.54 кБ)
  - `dist/assets/index-xyfPF6x7.js` — 3 856.33 кБ (gzip: 2 298.00 кБ); в будущем стоит разбить логику на ленивые модули (`build.rollupOptions.output.manualChunks`) и/или lazy-роутинг.
  - Шрифты Manrope (`dist/assets/manrope-*.woff2`) лежат в диапазоне 8.5–24.8 кБ и подключаются через `@font-face`.

### Lighthouse (mobile/desktop)

- Lighthouse пока не запускался в этой среде (нет headless Chromium). По завершении Phase 6 запускайте команду вроде:

```
npx lighthouse http://localhost:5173/timeline --only-categories=performance,accessibility,best-practices --output=json --output-path=logs/lighthouse-timeline.json
```

- Записывайте результаты (LCP, FID/TBT, Accessibility, Best Practices) в `docs/processes/qa-smoke-log.md` и сверяйтесь с чеклистом.
- Если показатели ухудшатся после рефакторинга, фиксируйте причину и добавляйте ссылку на Lighthouse-отчёт в релизную заметку.
