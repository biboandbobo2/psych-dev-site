# QA / Smoke Log

| Дата | Сценарий / Команда | Результат | Ответственный | Примечания |
|------|--------------------|-----------|---------------|------------|
| 2025-11-10 | `npm run build` | Vite build завершился, есть warning по chunk > 500кБ, артефакты `dist/assets/index-xyfPF6x7.js` (gzip 2,298 кБ) | Aleksej | Предупреждение по chunk, без ошибок |
| 2025-11-10 | `npx lighthouse http://localhost:4173/timeline --only-categories=performance,accessibility,best-practices --output=json --output-path=logs/lighthouse-timeline.json` | Lighthouse сгенерировал JSON `logs/lighthouse-timeline.json` (Performance/Accessibility/Best Practices) | Aleksej | Сервер на `localhost:4173`, отчет сохраняется в `logs/`; следить за предупреждением о чанках >500кБ |
| 2025-11-10 | `npm run test` | Vitest: 10 файлов, 43 теста — ✅ | Codex AI | Локальная проверка после рефакторинга страниц периодов |
| 2025-11-21 | Ручное тестирование: навигация между курсами | ✅ Навигация сохраняется при переходах между страницами. Store синхронизируется с URL. localStorage работает корректно | Claude Sonnet 4.5 | Реализована мультикурсовая система с useCourseStore + localStorage. Тесты интегрированы с курсами. Добавлено отображение тестов на страницах занятий |

> Заполняйте таблицу после каждого запуска `npm run test`, `npm run build`, а также после ручных smoke-проверок (CRUD заметок, экспорт, создание события из заметки, сценарии администратора). Добавляйте ссылки на PR/коммиты в колонку «Примечания».
| 2025-12-14 | Research drawer + /research: manual smoke (drawer search, open all, filters) + npm run lint && npm run check-console && npm run check:init && npm test (targeted) && npm run build | ✅ | Codex AI | dev → local |
| 2025-12-14 | Build after chunk tweaks (event-icons manual chunk) | ✅ | Codex AI | npm run build |
| 2025-12-14 | Wikidata query expansion + tests + build | ✅ | Codex AI | npm run lint && npm run check-console && npm run check:init && npm test (targeted) && npm run build |
| 2026-03-07 | `npx vitest run src/utils/testRevealPolicy.test.ts src/components/tests/__tests__/TestQuestionScreen.test.tsx src/components/tests/__tests__/TestResultsScreen.test.tsx` | 8 тестов, 3 файла — ✅ | Codex AI | Проверка логики показа верных ответов для 1 уровня и скрытия обещания для 2+ уровней |
| 2026-03-07 | `npm run lint`, `npm run check-console`, `npm run check:init`, `npm run build` | ✅ | Codex AI | Сборка успешна; есть штатные предупреждения `baseline-browser-mapping` и `caniuse-lite`, без ошибок |
| 2026-03-07 | `npm test -- --run src/components/__tests__/SaveNoteAsEventButton.test.tsx src/components/ui/__tests__/BaseModal.test.tsx` и `npm test -- --run src/features/periods/components/VideoSection.test.tsx src/features/periods/components/VideoStudyNotesPanel.test.tsx src/types/notes.test.ts` | ✅ | Codex AI | Точечная проверка вынесенной формы заметки и нового режима `Видео + заметки` |
| 2026-03-07 | `npm run validate:full` | ✅ | Codex AI | `lint` + `check-console` + `check:init` + `build` + полный Vitest: 40 файлов, 391 тест; сборка успешна, есть штатные предупреждения `baseline-browser-mapping` и `caniuse-lite` |
| 2026-03-07 | `git push` → автоматический `pre-push validate` | ✅ | Codex AI | Перед публикацией ветки `feature/video-study-notes` хук повторно прогнал `lint` + `check-console` + `check:init` + `build`; push завершился успешно |
| 2026-03-07 | `npm test -- --run src/hooks/__tests__/useNotes.test.ts src/features/periods/components/VideoSection.test.tsx src/features/periods/components/VideoStudyNotesPanel.test.tsx` и `npm run validate` | ✅ | Codex AI | Исправление лишней realtime-подписки в inline-заметках и сохранения черновика при переключении режимов видео |
| 2026-03-07 | `npm test -- --run src/features/periods/components/VideoSection.test.tsx src/features/periods/components/VideoStudyNotesPanel.test.tsx src/hooks/__tests__/useNotes.test.ts` и `npm run validate` | ✅ | Codex AI | Упрощён UX режима конспекта: компактная кнопка, узкая колонка заметок слева, период и заголовок заметки задаются автоматически |
| 2026-03-07 | `npm test -- --run src/features/periods/components/VideoSection.test.tsx src/features/periods/components/VideoStudyNotesPanel.test.tsx src/hooks/__tests__/useNotes.test.ts` и `npm run validate` | ✅ | Codex AI | Режим конспекта перестроен в формат YouTube/Zoom: почти весь экран занимает видео, заметки вынесены в правую chat-like панель |
| 2026-03-08 | `npm test -- --run src/features/periods/components/VideoSection.test.tsx src/features/periods/components/VideoStudyNotesPanel.test.tsx src/hooks/__tests__/useNotes.test.ts` и `npm run validate` | ✅ | Codex AI | Режим конспекта переведён в настоящий fullscreen overlay через portal: видео занимает viewport, заметки вынесены в правую колонку, скролл страницы блокируется, черновик сохраняется между открытиями |
| 2026-03-08 | `npm test -- --run src/lib/__tests__/videoTranscripts.test.ts`, `npm run transcripts:import -- '--video=https://www.youtube.com/watch?v=dQw4w9WgXcQ' --dry-run --limit=1` и `npm run validate` | ✅ | Codex AI | Добавлены схема `videoTranscripts`, helper для `youtubeVideoId`/storage path и batch-импортёр транскриптов YouTube в Firestore + Storage; dry-run подтвердил успешное получение transcript |
| 2026-03-08 | `npm test -- --run scripts/lib/videoTranscriptImportArgs.test.ts scripts/lib/videoTranscriptTargets.test.ts src/lib/__tests__/videoTranscripts.test.ts`, `npm run transcripts:import -- '--video=https://www.youtube.com/watch?v=dQw4w9WgXcQ' --dry-run --limit=1` и `npm run validate` | ✅ | Codex AI | Рефакторинг импортёра транскриптов: общий YouTube helper, разбиение batch-скрипта на модули < 400 строк, script-tests включены в Vitest include |
| 2026-03-08 | `npm run transcripts:import`, проверка `videoTranscripts`/Storage, `npm test -- --run src/features/periods/components/VideoSection.test.tsx src/features/periods/components/VideoStudyNotesPanel.test.tsx src/lib/__tests__/videoTranscripts.test.ts` и `npm run validate` | ✅ | Codex AI | Выполнен реальный импорт transcript в Firebase: 45 документов, из них 42 `available` и 3 `unavailable`; в fullscreen-режим добавлено переключение правой панели между конспектом и transcript по кнопке в header |
| 2026-03-08 | `npm run validate`, `npm run deploy:storage` и проверка неавторизованного чтения transcript через Firebase Storage SDK | ✅ | Codex AI | Открыт публичный read-доступ к `video-transcripts/**` в `storage.rules`, чтобы transcript был доступен всем пользователям, которым доступно видео; клиентское чтение `video-transcripts/eGCP_Fk7AeU/transcript.v1.json` подтверждено |
| 2026-03-08 | Чтение Firebase Auth config через Identity Toolkit API, PATCH `authorizedDomains`, повторная проверка конфига | ✅ | Codex AI | В `psych-dev-site-prod` добавлен preview-домен `psych-dev-site-git-feature-video-2e3cc5-alexey-zykovs-projects.vercel.app` в список authorized domains для входа через Google во время тестирования ветки |
| 2026-03-08 | `npm test -- --run src/hooks/__tests__/useNotes.test.ts src/features/periods/components/VideoStudyNotesPanel.test.tsx src/features/periods/components/VideoSection.test.tsx` и `npm run validate` | ✅ | Codex AI | Ручные заметки переведены на универсальный контекст `course + lesson`: фильтрация через `periodKey`, выбор курса и занятия из опубликованного контента, сохранение `seminary` и других динамических занятий без статического age-range списка |
| 2026-03-08 | `npm test -- --run src/hooks/__tests__/useNotes.test.ts src/features/periods/components/VideoStudyNotesPanel.test.tsx src/features/periods/components/VideoSection.test.tsx` и `npm run validate` | ✅ | Codex AI | Конспект лекции переведён на подгрузку существующей lecture-note и debounce-автосохранение без очистки текста: повторное открытие лекции показывает прежний конспект, новый ввод дописывается в ту же запись |
| 2026-03-08 | `npm test -- --run src/features/periods/components/VideoStudyNotesPanel.test.tsx src/features/periods/components/VideoSection.test.tsx src/hooks/__tests__/useNotes.test.ts` и `npm run validate` | ✅ | Codex AI | Упрощён UI правой панели конспекта: удалён верхний информационный блок и ручная кнопка сохранения, статус заменён на tooltip-индикатор автосохранения |
| 2026-03-08 | `npm test -- --run src/features/periods/components/VideoStudyNotesPanel.test.tsx src/features/periods/components/VideoSection.test.tsx src/hooks/__tests__/useNotes.test.ts` и `npm run validate` | ✅ | Codex AI | Индикатор автосохранения перенесён внутрь рамки заметки в правый верхний угол, вместо нативного `title` добавлена отдельная hover/focus подсказка |
| 2026-03-08 | `npm test -- --run src/hooks/__tests__/useNotes.test.ts src/features/periods/components/VideoStudyNotesPanel.test.tsx src/features/periods/components/VideoSection.test.tsx` и `npm run validate` | ✅ | Codex AI | Исправлен цикл повторной загрузки lecture-note: автосохранение снова пишет в ту же запись, кнопка `Сохранить` возвращена, нижний статус больше не мигает `Загружаем прошлый конспект...` |
| 2026-03-09 | `npm test -- --run src/features/periods/components/VideoSection.test.tsx src/features/periods/components/VideoStudyNotesPanel.test.tsx src/hooks/__tests__/useNotes.test.ts` и `npm run validate` | ✅ | Codex AI | Локальный cleanup ветки: вынесены общие video resource links, удалён неиспользуемый `saving` state из study notes panel и введён alias для `updateNote` payload в `useNotes` |
| 2026-03-09 | `npm test -- --run src/features/periods/hooks/useTimestampedLectureDraft.test.ts src/hooks/__tests__/useNotes.test.ts src/features/periods/components/VideoStudyNotesPanel.test.tsx src/features/periods/components/VideoSection.test.tsx src/features/periods/utils/youtubePlayer.test.ts` и `npm run validate` | ✅ | Codex AI | Добавлен режим таймкодированного конспекта: lecture-note сохраняет `lectureSegments`, fullscreen-плеер читает `currentTime/paused` через YouTube IFrame API, таймкоды кликабельны и перематывают видео |
| 2026-03-09 | `npm test -- --run src/features/periods/components/VideoStudyNotesPanel.test.tsx src/features/periods/components/VideoSection.test.tsx src/features/periods/hooks/useTimestampedLectureDraft.test.ts src/hooks/__tests__/useNotes.test.ts` и `npm run validate` | ✅ | Codex AI | Исправлена деградация timestamped draft: ввод больше не дробится из-за parent-sync, правый notes-panel получил auto-grow textarea, вертикальный scroll и автопрокрутку вниз; локальный draft снова переживает закрытие overlay |
| 2026-03-09 | `npm test -- --run api/__tests__/assistant.test.ts` и `npm run validate:full` | ✅ | Codex AI | Исправлен Gemini mock в `api/__tests__/assistant.test.ts`: общий конструктабельный mock `@google/genai` и заглушка `console.error` убрали шумные `stderr`-логи при полном прогоне тестов |
| 2026-03-09 | `npm run validate:full`, `cd functions && npm test && npm run build` | ✅ | Codex AI | Вынесен shared transcript core для `scripts` и `functions`, добавлен weekly transcript refresh с retry/backoff, Firestore lock, job-run summary и Telegram report через существующий bot helper; root Vitest: 47 файлов / 414 тестов, functions Vitest: 7 файлов / 40 тестов |
| 2026-03-09 | `firebase deploy --only functions:weeklyTranscriptRefresh --project psych-dev-site-prod`, `gcloud functions describe weeklyTranscriptRefresh`, `gcloud scheduler jobs describe firebase-schedule-weeklyTranscriptRefresh-us-central1` | ✅ | Codex AI | Повторный targeted deploy после фикса timezone `Asia/Tbilisi`: функция `weeklyTranscriptRefresh` обновлена до `versionId=2`, scheduler job создан в `us-central1`, cron `0 9 * * 0`, timezone `Asia/Tbilisi`, state `ENABLED` |
| 2026-03-09 | `cd functions && npm test -- --run src/lib/telegram.test.ts src/lib/adminApp.test.ts src/transcriptRefreshReport.test.ts src/transcriptRefreshCandidates.test.ts && npm run build`, создание Secret Manager secrets `telegram-bot-token` / `telegram-chat-id`, deploy `functions:sendFeedback,functions:weeklyTranscriptRefresh`, manual Pub/Sub trigger weekly refresh | ✅ | Codex AI | Telegram переведён на единый Secret Manager source с fallback на env для локалки; `sendFeedback` обновлена до `versionId=8`, `weeklyTranscriptRefresh` до `versionId=6`, manual run завершился `status=completed` и Telegram report доставлен автоматически |
| 2026-03-09 | `npm run check:functions-runtime`, `npm run validate`, `cd functions && npm test && npm run build` | ✅ | Codex AI | Миграция functions на Node 22 подготовлена локально: runtime-guard блокирует откат, root/.nvmrc/firebase/functions config синхронизированы на `nodejs22`, сборка и тесты проходят |
| 2026-03-09 | `firebase deploy --only functions --project psych-dev-site-prod`, `gcloud functions describe sendFeedback`, `gcloud functions describe weeklyTranscriptRefresh`, `gcloud functions describe ingestBook --gen2`, `curl ...sendFeedback`, `gcloud pubsub topics publish firebase-schedule-weeklyTranscriptRefresh-us-central1`, проверка Firestore run `transcriptJobs/weeklyRefresh/runs` | ✅ | Codex AI | Все 18 функций переведены на `nodejs22`; smoke после деплоя успешен: `sendFeedback` вернул `success:true`, manual weekly refresh завершился `status=completed`, последний run `w6pqcu20q1TloAyFDawe` |
| 2026-03-09 | `npm run check:functions-runtime`, `cd functions && npm test -- --run src/lib/adminSeedCode.test.ts src/lib/telegram.test.ts src/index.test.ts && npm run build`, создание Secret Manager secret `admin-seed-code`, deploy `functions:seedAdmin`, `firebase functions:config:unset admin.seed_code`, `firebase functions:config:get admin`, `gcloud functions describe seedAdmin` | ✅ | Codex AI | `seedAdmin` переведён с `functions.config()` на Secret Manager; guard теперь блокирует новые использования legacy config в `functions/src`; старый runtime-config ключ `admin.seed_code` удалён, `seedAdmin` работает на `nodejs22` с `versionId=36`, коллекция `admin` в Runtime Config пустая |
| 2026-03-10 | `cd functions && npm test -- --run src/lectureRagChunker.test.ts src/lectureRagPersistence.test.ts && npm run build`, `npm run validate`, `npm run lectures:ingest -- --dry-run --limit=2` | ✅ | Codex AI | Этап 1 lecture-RAG: добавлены shared schema/chunking/persistence для `lecture_sources` и `lecture_chunks`, плюс backfill-скрипт `lectures:ingest`; dry-run ingestion отработал штатно и корректно пропустил первые unavailable transcript targets |
| 2026-03-10 | `npm test -- --run api/lectures.test.ts src/features/contentSearch/hooks/useContentSearch.test.tsx` и `npm run validate` | ✅ | Codex AI | Этап 2 lecture-RAG: добавлен public endpoint `/api/lectures` с `list/search/answer`, локальный pure-helper `buildCourseLessonPath` вынесен в `src/lib/courseLessonPaths.ts`, текущий transcript-search переведён на него без регрессий |
| 2026-03-10 | `npm test -- --run api/books.test.ts api/lectures.test.ts src/features/lectureSearch/components/LectureSelector.test.tsx src/features/lectureSearch/hooks/useLectureAnswer.test.ts src/features/contentSearch/hooks/useContentSearch.test.tsx` и `npm run validate` | ✅ | Codex AI | Этап 3 lecture-RAG UI: добавлен блок `ИИ по лекциям` на `/research`, selector курса/лекций и client hook для `/api/lectures`; embeddings model обновлён на `gemini-embedding-001`, lecture API получил lexical fallback на `videoTranscriptSearch/searchChunks` |
| 2026-03-10 | `npm run lectures:ingest -- --limit=1` | ⚠️ | Codex AI | Локальный backfill lecture embeddings не завершился: текущая машина получает `FAILED_PRECONDITION: User location is not supported for the API use.` от Gemini embeddings, поэтому runtime feature опирается на transcript-search fallback вместо заполненных `lecture_chunks` |
| 2026-03-11 | `cd functions && npm test -- --run src/lectureRagChunker.test.ts src/lectureRagPersistence.test.ts src/lectureRagRunner.test.ts && npm run build`, `npm run lectures:ingest -- --dry-run --limit=2`, `npm run validate` | ✅ | Codex AI | Lecture ingest переведён на cloud-trigger pattern: добавлена Cloud Function `ingestLectureRag`, общий shared runner для загрузки transcript/embeddings/save и обновлённый `lectures:ingest`, который теперь триггерит облачное выполнение вместо локального Gemini embeddings |
| 2026-03-11 | `npm test -- --run api/lectures.test.ts src/features/lectureSearch/hooks/useLectureAnswer.test.ts` и `npm run validate` | ✅ | Codex AI | AI-ответы по лекциям переведены в vector-only retrieval по `lecture_chunks`; transcript fallback оставлен только для `list/search`, а отсутствие prepared chunks теперь возвращает понятный ответ без скрытого lexical отката |
| 2026-03-11 | `npm test -- --run src/lib/__tests__/lazyWithReload.test.ts` и `npm run validate` | ✅ | Codex AI | Добавлен единый lazy-import recovery для stale Vite chunks после preview deploy: page-level lazy routes и lazy-компоненты Timeline теперь один раз перезагружают страницу вместо падения с `'text/html' is not a valid JavaScript MIME type` |
| 2026-03-11 | `npm test -- --run src/features/lectureSearch/components/LectureAnswer.test.tsx src/features/lectureSearch/hooks/useLectureAnswer.test.ts` и `npm run validate` | ✅ | Codex AI | Блок `ИИ по лекциям` поднят выше книг на `/research`, а lecture citations усилены в UI: отдельный таймкод-ссылка, более заметная карточка источника, claim и excerpt прямо в ответе |
| 2026-03-11 | `cd functions && npm test -- --run src/transcriptRefreshReport.test.ts && npm run build`, `firebase deploy --only functions:weeklyTranscriptRefresh --project psych-dev-site-prod`, ручной Pub/Sub trigger weekly refresh, проверка Firestore run `transcriptJobs/weeklyRefresh/runs/wZl7DmI2HdbgmTOztNFr` | ✅ | Codex AI | Weekly transcript report расширен явными pipeline-метриками `transcript/search/lecture AI`; продовая функция обновлена до `versionId=11`, ручной run завершился `status=completed`, summary записал новые поля и Telegram report отправился без ошибок |
| 2026-03-11 | `npm test -- --run api/lectures.test.ts src/features/lectureSearch/hooks/useLectureAnswer.test.ts src/features/lectureSearch/hooks/useLectureSources.test.ts`, `cd functions && npm test -- --run src/lectureRagRunner.test.ts && npm run build`, `npm run validate` | ✅ | Codex AI | Усилена безопасность lecture AI: `/api/lectures` теперь принимает только авторизованных пользователей, ограничивает CORS локальными/prod/preview origin-ами, не отдаёт наружу сырые server errors и мягко парсит JSON Gemini; `ingestLectureRag` защищён secret-заголовком, `getEmbedding()` теперь всегда делает truncate, а preload всех transcript chunks в клиент зафиксирован как техдолг в backlog |
| 2026-03-11 | `firebase deploy --only functions:ingestLectureRag --project psych-dev-site-prod`, `gcloud functions describe ingestLectureRag --gen2 --region=europe-west1`, `curl -X POST https://europe-west1-psych-dev-site-prod.cloudfunctions.net/ingestLectureRag -H X-Ingest-Secret:*** ...` | ✅ | Codex AI | Для `ingestLectureRag` создан Secret Manager secret `LECTURE_INGEST_SECRET`, функция обновлена до ревизии `ingestlecturerag-00004-bih` и post-deploy smoke через новый secret-заголовок вернул `ok:true`/`status:"skipped"` для `eGCP_Fk7AeU`, подтверждая рабочий защищённый HTTP ingest |
| 2026-03-11 | `npm test -- --run src/features/contentSearch/hooks/useTranscriptSearchChunks.test.ts src/features/contentSearch/hooks/useContentSearch.test.tsx` и `npm run validate` | ✅ | Codex AI | Глобальный transcript search перестал preload-ить весь `searchChunks` индекс в браузер: `useTranscriptSearchChunks` стал query-driven, ходит в `/api/transcript-search` только для текущего запроса и кеширует результаты по query вместо загрузки всех transcript chunks за сессию |
| 2026-03-11 | `npm test -- --run api/lectures.test.ts src/types/notes.test.ts src/features/periods/components/VideoTranscriptPanel.test.tsx`, `cd functions && npm run build`, `npm run validate` | ✅ | Codex AI | `api/lectures.ts` разрезан на локальные server-модули `api/lib/*` без регресса поведения; вынесены course/path config, auth/CORS/JSON runtime helper и transcript fallback-логика, а также убраны локальные дубликаты batched-write helper в shared persistence и форматирования таймкодов на клиенте |
| 2026-03-11 | `npm test -- --run src/features/lectureSearch/hooks/useLectureSources.test.ts` и `npm run validate` | ✅ | Codex AI | `useLectureSources` получил `AbortController`, отмену предыдущего `fetch` и cleanup при размонтировании; зависимость эффекта стабилизирована через `userId`, чтобы список lecture sources не перезагружался лишний раз и не попадал в race-condition |
| 2026-03-11 | `npm run typecheck:app`, `npm run typecheck:api`, `npm run typecheck:scripts`, `npm run typecheck:functions` и `npm run validate` | ✅ | Codex AI | Очередь 1 typecheck: введены отдельные TS-контуры для `app/api/tests/scripts`, root-конфиг переведён на modern Vite/Bundler baseline с alias `@/*`, `scripts/tsconfig` отвязан от старого `commonjs/rootDir`, а функции подтверждены как type-safe; после конфиговой чистки шум сократился до реальных app/api/script ошибок без смешения с test/runtime-конфигом |
| 2026-03-11 | `npm run typecheck:app`, `npm run typecheck:api`, `npm run typecheck:scripts`, `npm run typecheck:functions` и `npm run validate` | ✅ | Codex AI | Очередь 2 typecheck: устранены реальные app-type рассинхроны в заметках, тестовом редакторе, кнопках UI, dynamic course lessons, admin content и timeline-сигнатурах; `typecheck:app/api/scripts/functions` теперь зелёные. Legacy-скрипт `scripts/migrateAuthorsTests.ts`, завязанный на давно удалённые test data-файлы, временно исключён из общего `typecheck:scripts`, чтобы не смешивать мёртвую миграцию с текущим runtime-кодом |
| 2026-03-11 | `npm run typecheck:tests`, `npm run typecheck:scripts` и `npm run validate` | ✅ | Codex AI | Очередь 3 typecheck: тесты приведены к текущим timeline/auth/test typings, integration helper переведён на HTTP reset Firestore emulator, legacy migration script `scripts/migrateAuthorsTests.ts` удалён вместе с устаревшими docs-ссылками на `authorsTest*Data.ts`; `typecheck:tests` и `typecheck:scripts` теперь зелёные без специальных исключений под мёртвый код |
| 2026-03-12 | `npm run validate`, `npm test -- --run`, `cd functions && npm test -- --run`, `npm run typecheck:app`, `npm run typecheck:api`, `npm run typecheck:scripts`, `npm run typecheck:functions`, `npm run typecheck:tests` | ✅ | Codex AI | Полный review-pass перед документированием результатов: `validate` зелёный; root Vitest — 57 файлов / 452 теста; functions Vitest — 14 файлов / 62 теста; все TS-контуры зелёные. Build по-прежнему предупреждает о крупных чанках `vendor` (~925 kB) и `event-icons` (~4.7 MB) |
| 2026-03-12 | `npm run test:ci` | ❌ | Codex AI | Скрипт сломан: текущий Vitest не поддерживает `--runInBand`, команда падает с `CACError: Unknown option --runInBand`; требуется правка `package.json` |
| 2026-03-12 | `npm test -- --run src/hooks/useDynamicCourseLessons.test.ts src/hooks/__tests__/useActiveCourse.test.ts` и `npm run validate` | ✅ | Codex AI | Dynamic courses больше не переводят весь app shell в fullscreen splash: добавлены cache + prefetch занятий, локальный loading/error в sidebar и дефолтной навигации; targeted Vitest 2 файла / 9 тестов, validate зелёный |
| 2026-03-12 | `npm run typecheck:api`, `npm run typecheck:tests` и `npm test -- --run api/books.test.ts api/lectures.test.ts api/__tests__/assistant.test.ts api/__tests__/papers.test.ts` | ✅ | Codex AI | Fix для Vercel serverless TS-check: relative imports в `api/*` и `src/types/videoTranscripts.ts` переведены на NodeNext `.js`, а `api/lectures.ts` получил явные типы для mixed vector/fallback lecture matches; 4 API test files / 76 tests зелёные |
| 2026-03-12 | `npm test -- --run src/pages/timeline/persistence.test.ts src/hooks/__tests__/useTimeline.test.ts`, `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Таймлайн переведён на multi-canvas storage внутри `timelines/{userId}`: добавлены legacy-migration в первый холст, создание/переключение холстов в левой панели и укороченный scale slider; targeted Vitest 2 файла / 5 тестов, `typecheck:app` и `validate` зелёные |
| 2026-03-12 | `npm run validate` | ✅ | Codex AI | UI polish для multi-canvas timeline: кнопки `Выбор ТЛ` и `+` собраны в одну строку, hover tooltips добавлены, меню выбора холстов раскрывается вправо и остаётся видимым поверх левой панели; validate зелёный |
| 2026-03-12 | `npm test -- --run api/timeline-biography.test.ts src/pages/timeline/persistence.test.ts src/hooks/__tests__/useTimeline.test.ts`, `npm run typecheck:api`, `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Для пустого холста добавлен импорт биографии из Wikipedia через `/api/timeline-biography`: Gemini (`gemini-2.5-pro` с fallback на `gemini-2.5-flash`) строит multi-branch timeline, который подменяет только активный canvas; targeted Vitest 3 файла / 10 тестов, оба typecheck и validate зелёные |
| 2026-03-12 | `find api -type f | rg -v '\\.test\\.' | wc -l`, `npm run typecheck:api`, `npm test -- --run api/timeline-biography.test.ts api/lectures.test.ts`, `npm run validate` | ✅ | Codex AI | Vercel Hobby function-count fix: server-only helpers вынесены из `api/` в `server/api/`, а неиспользуемый `api/lectureTranscriptFallback.ts` удалён; runtime endpoints под `api/` сокращены до 7, API-тесты 2 файла / 23 теста и validate зелёные |
| 2026-03-12 | `npm test -- --run src/components/ui/__tests__/BaseModal.test.tsx api/timeline-biography.test.ts src/pages/timeline/persistence.test.ts src/hooks/__tests__/useTimeline.test.ts`, `npm run validate` | ✅ | Codex AI | Fix для кнопки импорта биографии: `TimelineBiographyImportModal` больше не lazy-chunk, а `BaseModal` рендерится через portal с высоким z-index, поэтому модалка стабильно открывается поверх timeline-панелей и preview-кеша |
| 2026-03-12 | `npm run check-console`, `npm run validate` | ✅ | Codex AI | Убран legacy debug-мусор из `SaveEventAsNoteButton`: прямые `console.*` и `DETECTOR`-логи больше не забивают консоль timeline и не маскируют реальные ошибки импорта биографии |
| 2026-03-12 | `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Импорт биографии переведён из модалки во встроенный inline-блок левой панели: клик по CTA теперь сразу раскрывает форму URL на месте, а `Timeline` дополнительно пишет подробные `debugLog/debugError` по open/request/response flow |
| 2026-03-12 | `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Для точной диагностики biography import добавлен видимый debug-блок в левой панели: журнал событий, `pointerdown/click` capture и `elementFromPoint()` probe по центру кнопки, чтобы понять наверняка доходит ли клик и не перекрыта ли кнопка другим слоем |
| 2026-03-12 | `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Диагностика левой панели сжата, чтобы не ломать layout timeline, и дополнена отдельными React/native/document click-счётчиками для `Выход` и `+`, чтобы сравнить доставку событий biography CTA против соседних элементов панели |
| 2026-03-12 | `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Диагностика timeline усилена для iPad/Safari: левая панель теперь считает события на уровнях `aside`/`Выход`/`Выбор ТЛ`/`+`/`Скачать`, пишет компактный raw-log control signals и отдельно показывает видимый статус/журнал export-flow, чтобы изолировать проблемы кликов от проблем PDF-экспорта |
| 2026-03-13 | `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Финализирована неразрушающая диагностика timeline: тяжёлый debug-блок вынесен из нормального layout в toggleable popover, в панели оставлена только компактная status-строка, а `Выход` переведён с `Link` на явный `navigate('/profile')`, чтобы исключить iPad/Safari-анкерные странности при рабочем click-flow |
| 2026-03-13 | `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Для biography CTA на iPad/Safari добавлен обходной фикс через делегирование клика на уровне всей левой панели: теперь `bio-import` открывается по panel-click даже если target-level handler на самой кнопке не срабатывает; raw-log также пишет семантические `data-panel-action` (`bio-import`, `exit`, `select`, `create`, `download`, `dbg-toggle`) вместо безличного `button` |
| 2026-03-13 | `npm test -- --run src/pages/timeline/hooks/__tests__/useTimelineHistory.test.ts`, `npm run validate` | ✅ | Codex AI | Подтверждён и исправлен self-reset regression в timeline: `useTimelineHistory` переведён на стабильные callback-обработчики через refs, чтобы `resetTransientTimelineUi` не пересоздавался на каждом рендере и не схлопывал biography import сразу после открытия; `Выход` также возвращён ближе к `main` как `Link to="/profile"` |
| 2026-03-13 | `npm test -- --run api/timeline-biography.test.ts`, `npm run validate` | ✅ | Codex AI | `/api/timeline-biography` усилен для preview/runtime: server Gemini key теперь ищется через fallback-цепочку (`GEMINI_API_KEY` / `MY_GEMINI_KEY` / `GOOGLE_API_KEY` / `VITE_GEMINI_KEY`), а 500-ошибки нормализуются в понятные причины вроде отсутствующего ключа, квоты, недоступной модели или некорректного Gemini JSON-ответа |
| 2026-03-13 | `npm test -- --run api/timeline-biography.test.ts`, `npm run validate` | ✅ | Codex AI | Парсер ответа Gemini для biography import стал устойчивее к реальному runtime-формату: теперь `/api/timeline-biography` умеет собирать JSON из fenced blocks, убирать trailing commas и fallback-ить на `candidates[].content.parts[].text`, если `result.text` пустой |
| 2026-03-13 | `npm test -- --run api/timeline-biography.test.ts`, `npm run validate` | ✅ | Codex AI | Для biography import добавлен relaxed retry без `responseJsonSchema`: если structured Gemini output не парсится, endpoint автоматически повторяет запрос в plain-text JSON режиме на том же model-pass и только потом переключается на fallback-модель |
| 2026-03-14 | `npm test -- --run api/timeline-biography.test.ts`, `npm run validate` | ✅ | Codex AI | Structured schema для biography import упрощена под Gemini: убран `anyOf`, `selectedPeriodization` переведён на `type: ["string", "null"]`, а лишняя schema-строгость (`additionalProperties: false`, жёсткие length/item bounds) снята в пользу server-side sanitize/normalization |
| 2026-03-14 | `npm test -- --run api/timeline-biography.test.ts`, `npm run validate` | ✅ | Codex AI | Для biography import добавлен аварийный line-based fallback: если обе JSON-стратегии Gemini на обеих моделях не возвращают валидный plan, `/api/timeline-biography` повторяет запрос в plain-text TAB-формате и всё равно собирает рабочий timeline из построчного ответа |
| 2026-03-14 | `npm test -- --run api/timeline-biography.test.ts`, `npm run validate` | ✅ | Codex AI | Biography import больше не принимает почти пустой Gemini plan как успех: сервер обогащает бедный ответ эвристиками из Wikipedia extract (birth details, хронологические main events, тематические branches), возвращает `planDiagnostics/timelineStats` в API-meta и роняет запрос, если после нормализации событий всё ещё нет |
| 2026-03-14 | `npm test -- --run src/pages/timeline/persistence.test.ts`, `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Для пустого холста добавлен второй способ импорта: кроме Wikipedia URL теперь можно загрузить готовый `.json` в формате `TimelineData`; shared normalizer чистит битые nodes/edges и используется как для JSON-файла, так и для runtime-подмены активного холста |
| 2026-03-14 | `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Исправлен timeline render/save после JSON-импорта: ветки теперь получают явный горизонтальный коннектор от родительского события к vertical lane, а autosave `timelines/{userId}` очищает `undefined` через `removeUndefined(...)`, чтобы Firestore больше не падал с `Unsupported field value: undefined` |
| 2026-03-14 | `npm run typecheck:api`, `npm test -- --run api/timeline-biography.test.ts`, `npm run validate` | ✅ | Codex AI | Для Gemini biography import усилены prompt/quality guards: зафиксированы adaptive density, coverage всей жизни, обязательный terminal event для умерших, запрет дублей main/branch и generic labels; server-side enrichment теперь жёстче дедуплицирует факты, держит ветки в одной сфере и fallback-ит на heuristics, если модель теряет позднюю жизнь |
| 2026-03-14 | `npm run typecheck:api`, `npm test -- --run api/timeline-biography.test.ts`, `npm run validate` | ✅ | Codex AI | Biography import переведён на staged Gemini pipeline: сначала facts extraction, затем draft plan по facts + few-shot exemplar, затем quality review/fix pass по checklist; API теперь возвращает ещё и `factsModel`/`stageDiagnostics`, а guide `timeline.md` описывает каскад и debugging surface |
| 2026-03-14 | `npm run typecheck:api`, `npm run typecheck:app`, `npm test -- --run api/timeline-biography.test.ts src/pages/timeline/persistence.test.ts`, `npm run validate` | ✅ | Codex AI | Усилена observability и fallback-качество biography import: сервер теперь пишет `debugError/debugLog` по каждому stage/fallback, клиент показывает `source`/`factsModel`/`reviewApplied`/`reviewIssues` в import-UI, heuristic fallback лучше распознаёт `place/career/death` паттерны, а JSON/timeline import больше не раздувает `ageMax` до 100 по умолчанию |
| 2026-03-14 | `npm test -- --run src/lib/__tests__/geminiKey.test.ts`, `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Исправлен клиентский BYOK header-path для Safari/браузерных `fetch` ошибок вида `The string did not match the expected pattern`: Gemini API key теперь санитизируется единообразно при чтении из store и перед всеми запросами (`Timeline`, lecture AI, research AI, key validation), а import biography пишет более точный error-context при падении до сети |
| 2026-03-14 | `npm run typecheck:api`, `npm run typecheck:app`, `npm test -- --run api/timeline-biography.test.ts`, `npm run validate` | ✅ | Codex AI | Biography import усилен под `gemini-2.5-flash`: progress-modal принудительно открывается до запуска pipeline, server-контур `timelineBiography` разрезан на отдельные модули (`Types/Wikipedia/Prompts/Heuristics/Quality`), prompt/facts extraction добирают раннее детство и social-thread, а quality-layer отбрасывает пустые ветки и нелепые якоря вроде `education` от рождения |
| 2026-03-15 | `npm test -- --run api/timeline-biography.test.ts src/pages/timeline/components/__tests__/BiographyImportModal.test.tsx`, `npm run typecheck:app`, `npm run typecheck:api` | ✅ | Codex AI | После регресса biography import добавлен safe fallback вместо hard-fail в `enrichBiographyPlan`, плюс регрессионные тесты на слабый `gemini-2.5-flash` output с веткой `education` от рождения и на UI progress/error/success states у `BiographyImportModal` |
| 2026-03-15 | `npm test -- --run src/lib/__tests__/geminiKey.test.ts`, `npm run typecheck:app`, `npm run validate` | ✅ | Codex AI | Для Safari-подобного client-side `SyntaxError: The string did not match the expected pattern` усилена санитизация Gemini BYOK key (кавычки, zero-width, не-ASCII/служебные символы), а biography import в `Timeline.tsx` теперь автоматически повторяет запрос без optional `X-Gemini-Api-Key`, если браузер роняет первый `fetch` на header-pattern ошибке |
