# Бэклог по результатам аудита (январь 2025)

> 🔔 **Легенда:**  
> • **P** – приоритет (H = High, M = Medium, L = Low)  
> • **E** – ориентировочная трудоёмкость (S = 0.5–1 дн., M = 1–3 дн., L = >3 дн.)  
> • каждая задача разбита на пошаговые инструкции, чтобы её мог выполнить даже  агент. Если задача заметно усложняет поддержку, это явно указано.

---

## 1. 🔐 Безопасность доступа и ролей

### 1.1 [P: H | E: M] Удалить «одноразовое» повышение до админа из клиента
1. **Очистка фронтенда**
   - [ ] Выпилить чтение `VITE_ADMIN_SEED_CODE` и компонент `MakeMeAdmin` из `src/pages/Admin.tsx`.
   - [ ] Проверить, что переменная окружения больше не используется (поиск по проекту).
2. **Серверная логика**
   - [ ] Оставить seed-код только в `functions.config().admin.seed_code`.
   - [ ] Подготовить инструкцию для владельца проекта по обновлению секретов.
3. **Ротация и документация**
   - [ ] Сменить seed-код в Firebase Functions (иначе у злоумышленника остаётся рабочий ключ).
   - [ ] Добавить в README/вики новый порядок выдачи прав.
4. **Архитектура**
   - [ ] Обновить раздел `Security, Roles & Logging` в `docs/ARCHITECTURE_GUIDELINES.md` (описать новый процесс и запрет на хранение seed-кода в клиенте).

> ⚠️ Если оставить seed-код в клиенте, дальнейший аудит безопасности бессмыслен.

### 1.2 [P: H | E: S] Разрешить super-admin запускать служебные функции
1. [ ] В `functions/src/index.ts` обновить `ensureAdmin`, допускающий `role === 'admin' || role === 'super-admin'`.
2. [ ] Пересмотреть `makeUserAdmin`, `setRole`, `runVerify`, `runReconcile` и другие callable – они должны работать для обоих ролей.
3. [ ] Добавить тест в функции (эмуляторы Firebase) или хотя бы ручной сценарий проверки (описание шагов).
4. [ ] Зафиксировать требование в `ARCHITECTURE_GUIDELINES.md` (Access Control): `ensureAdmin` и все callable-функции принимают обе роли.

### 1.3 [P: H | E: S] Привести Storage rules и UI к единой политике ролей
1. [ ] В `storage.rules` разрешить запись для ролей `admin` и `super-admin`.
2. [ ] В `src/pages/UploadAsset.tsx` и `UploadAsset`-хелперах заменить проверки `role !== 'admin'` на «множество ролей».
3. [ ] Проверить загрузку под super-admin через Firebase Storage Emulator (желательно) или staging-проект.
4. [ ] Обновить `ARCHITECTURE_GUIDELINES.md` (Access Control) — прописать, что изменения в ролях требуют синхронных апдейтов UI + правил.

---

## 2. 🛡️ Логи и приватность

### 2.1 [P: H | E: M] Очистить продакшен-логирование чувствительных данных
1. **Сбор списка логов**
   - [ ] Поиск `console.` и `🔍`/`✅`/`❌` эмодзи в `src` и `functions`.
   - [ ] Классификация: токены/PII (обязательно убрать) vs полезные dev-логи.
2. **Внедрение dev-флага**
   - [ ] Создать утилиту `src/lib/debug.ts` с проверкой `import.meta.env.DEVLOG === 'true'`.
   - [ ] Завернуть разрешённые логи в условие, остальные удалить.
3. **Автоматический контроль**
   - [ ] Добавить ESLint rule (`no-restricted-syntax`) или простую Git-хук проверку, запрещающую `console.log` в `src/lib`, `src/pages`, `functions/src` (кроме явно перечисленных файлов).
4. **Документация**
   - [ ] Дополнить раздел Logging & Privacy в `ARCHITECTURE_GUIDELINES.md` (упоминание `debug`-утилиты и запрета на PII).

> 📌 После чистки провести smoke-тест, чтобы убедиться, что в браузере нет утечек токенов и пользовательских данных.

---

## 3. 📊 Данные и UX

### 3.1 [P: M | E: S] Исправить статистику администратора в профиле
1. [ ] Пересчитать `totalPeriods` и `publishedPeriods` корректно (учитывать реальное количество документов + интро).
2. [ ] Для подсчёта админов использовать либо фильтрацию по роли с ограничением (не грузить всю коллекцию), либо Cloud Function.
3. [ ] Добавить unit-тест (например, `ProfileStats.test.tsx`) с моками Firestore.

### 3.2 [P: M | E: S] Восстановить CSV-режим или удалить переключатель
1. [ ] Решить судьбу CSV-режима (созвон/решение продукта).
2. Если **нужен**:
   - [ ] Вернуть `public/content/periods.csv`, `intro.csv` или обновить пути.
   - [ ] Прогнать `scripts/check-embeds`, `scripts/verifyContent` в CSV-режиме.
3. Если **не нужен**:
   - [ ] Удалить `useDataSource`, CSV-хуки, команды `scripts/transform`/`reconcileContent`.
   - [ ] Обновить документацию (README, Admin инструкции).

> ⚠️ Оставлять неработающий переключатель опасно: администратор может случайно включить его и «потерять» контент.

---

## 4. 🚀 Производительность и сборка

### 4.1 [P: M | E: L] Миграция на ленивую загрузку (см. `docs/lazy-loading-migration.md`)
1. [ ] Следовать поэтапному плану (5 этапов) из отдельного документа.
2. [ ] После внедрения сравнить размеры бандлов и метрики Lighthouse с этапом 1.1.
3. [ ] Если какая-либо страница критична и ленивый импорт ухудшает UX (например, модалки), зафиксировать это в документации, чтобы новые разработчики не «оптимизировали» её повторно.
4. [ ] Обновить `ARCHITECTURE_GUIDELINES.md` и README (раздел о сборке) — описать policy по ленивой загрузке и целевые размеры чанков.

> ⚠️ Слишком агрессивное деление на чанки усложнит поддержку. Руководствуйтесь списком «Рекомендуемых ленивых модулей» в сопроводительном документе.

---

## 5. 🧪 Тестирование и QA (новые пункты)

### 5.1 [P: H | E: M] Стандартизировать запуск unit-тестов
1. [ ] Исправить команду `npm run test -- --runInBand` (либо удалить флаг, либо добавить alias).  
2. [ ] Настроить GitHub Actions / CI:  
   - шаг `npm ci`,  
   - `npm run lint` (когда появится конфиг для TS/JS),  
   - `npm run test`,  
   - `npm run build`.
3. [ ] Задокументировать команды в README и `docs/TESTS_SYSTEM_GUIDE.md`.

### 5.2 [P: M | E: L] Добавить интеграционные тесты с Firebase эмуляторами
1. [ ] Настроить локальные эмуляторы Firestore/Auth/Storage (`firebase emulators:start --only firestore,auth,storage`).  
2. [ ] Написать тесты (Vitest или Jest) для критических хелперов: `firestoreHelpers`, `tests`, `useAuthStore` (определение роли).  
3. [ ] Добавить npm-скрипт `test:integration` и упомянуть его в CI (по желанию – nightly).
4. [ ] Обновить `docs/TESTS_SYSTEM_GUIDE.md` (как запускать эмуляторы и интеграционные сценарии).

### 5.3 [P: M | E: L] Покрыть основные пользовательские сценарии E2E
1. [ ] Выбрать инструмент (Cypress/Playwright).  
2. [ ] Написать сценарии: вход → просмотр периодов, прохождение теста, создание заметки, работа таймлайна, CRUD администратора.  
3. [ ] Добавить запуск E2E в CI по расписанию (ежедневно/по тегу).  
4. [ ] Документировать процесс в `docs/TESTS_SYSTEM_GUIDE.md` (инструменты, структура тестов, запуск команд).

> 🧩 Эти задачи заметно повышают надёжность, но требуют поддержки инфраструктуры (эмуляторы, среды тестирования).

---

## 6. 🧹 Качество кода и архитектура

### 6.1 [P: M | E: M] Удалить устаревшие режимы и дублирующийся код
1. [ ] Проанализировать файлы `src/lib/usePeriods.js` vs `src/hooks/usePeriods.ts` — оставить единую реализацию.  
2. [ ] Проверить, нет ли старых UI (например, `AuthProvider` помечен как deprecated, но всё ещё используется).  
3. [ ] Очистить репозиторий от неиспользуемых скриптов или перенести их в `scripts/archive/`.

### 6.2 [P: M | E: S] Централизовать обработку ошибок
1. [ ] Добавить `ErrorBoundary` на уровне приложения.  
2. [ ] Ввести утилиту для перехвата ошибок Firestore/Firebase (логирование + пользовательский тост).  
3. [ ] Обновить ключевые хуки (`useNotes`, `useTimelineState`, `useAuthStore`) – вместо `alert` использовать централизованный механизм уведомлений.

### 6.3 [P: L | E: M] Метрики и мониторинг
1. [ ] Внедрить базовый logger/telemetry (Sentry, Firebase Analytics, PostHog — по выбору).  
2. [ ] Отслеживать критические события: ошибки авторизации, проваленные загрузки, неуспешные записи в Firestore.  
3. [ ] Добавить раздел «observability» в документацию (как читать алерты, где смотреть логи).

> ❗Добавление аналитики усложняет поддержку (нужно следить за конфигурацией и безопасности), но сильно помогает при регрессиях.

---

## 7. 📘 Документация и процессы

### 7.1 [P: M | E: S] Вести QA/Smoke-лог
- [ ] Создать `docs/qa-smoke-log.md` с колонками (дата, сценарий, команда, результат, ответственный).  
- [ ] После каждого `npm run test`, `npm run build` и ручного smoke (CRUD заметок, экспорт, создание события) фиксировать результат и ссылку на коммит.  
- [ ] Добавить ссылку на лог в `PLANS_OVERVIEW.md` и README, чтобы новый разработчик сразу видел историю проверок.

> 📝 Требование отмечено в `PLANS_OVERVIEW.md` и `REFRACTORING_ARCHIVE.md`; без лога не видно, что Phase 6 QA реально выполняется.

### 7.2 [P: M | E: S] Синхронизировать Timeline документацию
- [ ] Сравнить `docs/TIMELINE_REFACTORING_PLAN.md` и `docs/TimelineGuide.md`; привести описание UI/компонентов к единому виду.  
- [ ] В `TimelineGuide.md` добавить раздел с новой структурой (компоненты, хуки, экспорт) и ссылку на чеклист Phase 6.  
- [ ] Задокументировать свежие результаты `npm run build`/Lighthouse для таймлайна, как требует план (раздел Phase 6).

### 7.3 [P: M | E: S] Поддерживать тестовую документацию и статику
- [ ] Актуализировать `docs/TESTS_SYSTEM_GUIDE.md`, чтобы он отражал текущую архитектуру (TestsPage, DynamicTest, Zustand store).  
- [ ] Добавить npm-скрипт `ts:prune` и инструкцию в README (`npx ts-prune` → как читать отчёт).  
- [ ] В README явно сослаться на `ARCHITECTURE_GUIDELINES.md` и `TESTS_SYSTEM_GUIDE.md`, требуя прочтения перед новыми задачами/рефакторингами.

---

_После завершения каждой задачи ставьте ✅, добавляйте ссылку на PR/коммит и краткое резюме/регрессии (если были)._
