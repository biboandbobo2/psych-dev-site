# Бэклог по результатам аудита (январь 2025)

> 🔔 **Легенда:**  
> • **P** – приоритет (H = High, M = Medium, L = Low)  
> • **E** – ориентировочная трудоёмкость (S = 0.5–1 дн., M = 1–3 дн., L = >3 дн.)  
> • каждая задача разбита на пошаговые инструкции, чтобы её мог выполнить даже  агент. Если задача заметно усложняет поддержку, это явно указано.

---

## 1. 🔐 Безопасность доступа и ролей

### 1.1 [P: H | E: M] Удалить «одноразовое» повышение до админа из клиента
1. **Очистка фронтенда**
   - [x] Удалено чтение `VITE_ADMIN_SEED_CODE` и `MakeMeAdmin` в `src/pages/Admin.tsx`. Теперь очевидно: права выдаются только сервером.
   - [x] Переменная окружения больше не используется в проекте.
2. **Серверная логика**
   - [x] Seed-код хранится только в `functions.config().admin.seed_code`; команды `seedAdmin`/`ensureAdmin` проверяют роли (`admin`/`super-admin`), клиент не видит код.
   - [x] Документы (README, Render/Render setup, Architecture Guidelines, audit backlog) уточняют процесс обновления секретов и выдачи прав.
3. **Ротация и документация**
   - [x] В документации (`README.md`, `docs/RENDER_SETUP.md`) описан новый порядок: owner/super-admin обновляет seed и вызывает `seedAdmin`, пользователь с admin-роли больше не вводит код.
   - [x] Добавлены ссылки на процессы в `docs/QA` и `docs/audit-backlog.md:7.2`.
4. **Архитектура**
   - [x] `docs/ARCHITECTURE_GUIDELINES.md` (Access Control) фиксирует, что seed-код хранится на сервере, клиент не запрашивает его; ссылка на `docs/audit-backlog.md` упоминает процесс.

> ⚠️ Seed-код запрещено держать в клиенте; все шаги описаны в документах выше.

### 1.2 [P: H | E: S] Разрешить super-admin запускать служебные функции
1. [x] В `functions/src/index.ts` обновить `ensureAdmin`, допускающий `role === 'admin' || role === 'super-admin'`.
2. [x] Пересмотреть `makeUserAdmin`, `setRole`, `runVerify`, `runReconcile` и другие callable — все они используют `ensureAdmin`, поэтому принимают обе роли.
3. [x] Автотест `functions/src/index.test.ts` (запуск `cd functions && npm run test`) проверяет, что `ensureAdmin` виде корректно админов/супер-админов, и теперь он проходит в CI.
4. [x] Ручной сценарий проверки: super-admin вызывает `runVerify`/`runReconcile` с валидным токеном и видит `ok: true`/`plan` без ошибок, отчёт сохраняется в Firestore.
4. [x] Документировать (см. `docs/ARCHITECTURE_GUIDELINES.md` → Access Control) что `ensureAdmin` принимает обе роли, и что процессы проверки/ротации обновлены в бэклоге.

### 1.3 [P: H | E: S] Привести Storage rules и UI к единой политике ролей
1. [x] `storage.rules` теперь использует helper `isAdminOrSuperAdmin()` — админы и супер-админы могут писать в `assets/` и `tests/**`.
2. [x] `UploadAsset` проверяет `token.claims.role` на множественную роль и оповещает, что только админы/супер-админы могут загружать.
3. [x] При ручной проверке (Storage Emulator или staging) super-admin может загрузить ассет, аналогично admin.
4. [x] `docs/ARCHITECTURE_GUIDELINES.md` обновлён: упомянута синхронизация Storage Rules и UI, а изменения описаны в backlog.

---

## 2. 🛡️ Логи и приватность

### 2.1 [P: H | E: M] Очистить продакшен-логирование чувствительных данных
1. **Сбор списка логов**
   - [x] Поиск `console.` и `🔍`/`✅`/`❌` эмодзи в `src` и `functions` (в частности, Admin/UploadAsset и callable-функции).
   - [x] Классификация: токены/PII выделены, а корректные dev-логи перенесены в новые обёртки.
2. **Внедрение dev-флага**
   - [x] Создать утилиту `src/lib/debug.ts` / `functions/src/lib/debug.ts`, добавить `src/env.d.ts`, а `vite.config.js` открывает `DEVLOG`.
   - [x] Завернуть разрешённые логи в `debugLog`/`functionsDebugLog`, остальные убрать.
3. **Автоматический контроль**
   - [x] Добавили husky `pre-commit`, `scripts/check-console.cjs` и `npm run check-console`, чтобы staged-файлы в `src/`/`functions/src/` не содержали raw `console.*` (исключаем `debug.ts`/`scripts/**`).
4. **Документация**
   - [x] Дополнить раздел Logging & Privacy в `ARCHITECTURE_GUIDELINES.md` (упоминание `debug`-утилиты, флага `DEVLOG` и запрета на PII).

> 📌 После чистки провести smoke-тест, чтобы убедиться, что в браузере нет утечек токенов и пользовательских данных.

---

## 3. 📊 Данные и UX

### 3.1 [P: M | E: S] Исправить статистику администратора в профиле
1. [x] Пересчитать `totalPeriods` и `publishedPeriods` корректно (учитывать реальное количество документов).
2. [x] Для подсчёта админов использовать либо фильтрацию по роли с ограничением (не грузить всю коллекцию), либо Cloud Function.
3. [x] Добавить unit-тест (например, `ProfileStats.test.tsx`) с моками Firestore.

### 3.2 [P: M | E: S] Восстановить CSV-режим или удалить переключатель
1. [x] Решено: CSV-режим не нужен, интерфейс и скрипты работают напрямую с Firestore.
2. Если **нужен** — уже неактуально: файлов `public/content/*.csv` больше нет, `scripts/transform`/`verify`/`reconcile` удалены.
3. Если **не нужен**:
   - [x] Удалены `useDataSource`, CSV-хуки, `AdminImport` и навигационный переключатель.
   - [x] Удалены скрипты `transform`, `verify`, `reconcile`, `check:embeds`, и `papaparse` больше не в зависимостях.
   - [x] Документация обновлена (README, plans, architecture guides, audit backlog).

> ⚠️ Оставлять переключатель бесполезно; теперь единственный источник контента — Firestore.

---

## 4. 🚀 Производительность и сборка

### 4.1 [P: M | E: L] Миграция на ленивую загрузку (см. `docs/lazy-loading-migration.md`)
1. [x] Следовать поэтапному плану (5 этапов) из отдельного документа.
2. [x] После внедрения сравнить размеры бандлов и метрики Lighthouse с этапом 1.1 — результаты зафиксированы в `docs/perf-metrics.md`, `docs/lazy-loading-migration.md` и `README.md`.
3. [x] Если какая-либо страница критична и ленивый импорт ухудшает UX (например, модалки), зафиксировать это в документации, чтобы новые разработчики не «оптимизировали» её повторно (документация описывает PageLoader, fallback, риски для Timeline).
4. [x] Обновить `ARCHITECTURE_GUIDELINES.md` и README (раздел о сборке) — описать policy по ленивой загрузке и целевые размеры чанков.
5. [ ] Завершить дробление core `timeline` chunk — детальный план в `docs/timeline-core-split.md`, связанный пункт 4.4 в `docs/lazy-loading-migration.md`. Осталось вынести хук-логику/экспорт в lazy-модули, провести `npm run build`, зафиксировать размеры и проверить (см. раздел 4.4). 

> ⚠️ Слишком агрессивное деление на чанки усложнит поддержку. Руководствуйтесь списком «Рекомендуемых ленивых модулей» в сопроводительном документе.

---

## 5. 🧪 Тестирование и QA (новые пункты)

### 5.1 [P: H | E: M] Стандартизировать запуск unit-тестов
1. [x] Исправить команду `npm run test -- --runInBand` (либо удалить флаг, либо добавить alias).  
   - Добавлен `npm run test:ci` (`vitest --runInBand`) как alias без лишних флагов.
2. [x] Настроить GitHub Actions / CI:  
   - шаг `npm ci`,  
   - `npm run lint` (когда появится конфиг для TS/JS),  
   - `npm run test`,  
   - `npm run build`.
   - В `.github/workflows/ci.yml` прогоняются эти шаги на `push/pull_request`.
3. [x] Задокументировать команды в README и `docs/TESTS_SYSTEM_GUIDE.md`.
   - README и `docs/TESTS_SYSTEM_GUIDE.md` описывают `npm run test:ci`, `npm run lint`, `npm run test` и `npm run build`, а также упоминают новую CI pipeline.

### 5.2 [P: M | E: L] Добавить интеграционные тесты с Firebase эмуляторами
1. **Инфраструктура**
   - [x] Установить/обновить Firebase CLI.
   - [x] Настроить локальные эмуляторы Firestore/Auth/Storage (`firebase emulators:start --only firestore,auth,storage`).
   - [x] Создать конфиг `firebase.json`/`firestore.rules` для режима тестов (минимальные права).
     - `tests/integration/firebase.test.json` содержит конфигурацию с `psych-dev-site-test`.
     - `tests/integration/firestore.rules` и `tests/integration/storage.rules` — облегчённые правила доступа.
     - `npm run firebase:emulators:start` стартует эмуляторы с этой конфигурацией.
2. **Тестовый каркас**
   - [x] Создать `tests/integration/` с базовым helper (инициализация эмуляторов, очистка данных).
     - `tests/integration/helper.ts` экспортирует `initializeIntegrationApp` и `resetIntegrationData`, чтобы тестам не приходилось вручную дёргать `clearFirestoreData`/`admin.auth()`.
   - [x] Добавить npm-скрипт `test:integration` (vitest/jest + `--runInBand`).
   - [ ] Настроить GitHub Actions (опционально nightly) для запуска интеграций.
3. **Сценарии**
   - [x] Написать тесты для `firestoreHelpers` (CRUD периодов, topics).
     - `tests/integration/firestoreHelpers.test.ts` проверяет canonical id, фильтрацию `published`, удаление.
     - `tests/integration/topics.test.ts` используется для CRUD с `topics` (add/update/delete).
   - [x] Добавить тесты для `tests.ts` (создание теста, prerequisite, результаты).
     - `tests/integration/testsWorkflow.test.ts` создаёт тесты с `prerequisiteTestId`, публикует/снимает их, сохраняет `testResults` и группирует попытки.
   - [x] Покрыть `useAuthStore` (назначение ролей, супер-админ).
     - `tests/integration/authStore.test.ts` проверяет булевы флаги при ролях `student`, `admin`, `super-admin`.
4. **Документация**
   - [x] Обновить `docs/TESTS_SYSTEM_GUIDE.md`: как запускать эмуляторы, как устроены интеграционные сценарии.

### 5.3 [P: M | E: L] Покрыть основные пользовательские сценарии E2E
1. **Выбор и настройка инструмента**
   - [ ] Определиться с Cypress или Playwright.
   - [ ] Создать базовый проект (`npx playwright init` или `npx cypress open`), добавить npm-скрипт `test:e2e`.
   - [ ] Настроить тестовые данные (seed в Firestore/Storage для e2e).
2. **Сценарии**
   - [ ] Login + просмотр периодов.
   - [ ] Прохождение теста (включая prerequisite).
   - [ ] CRUD заметок + экспорт.
   - [ ] Работа таймлайна (создание, перемещение события).
   - [ ] Админ-флоу (назначение ролей, редактирование контента).
3. **CI и отчётность**
   - [ ] Добавить запуск e2e в CI (по расписанию или тегу), сохранять видео/скриншоты артефактами.
   - [ ] Настроить отчёт о падениях (Slack/email).
4. **Документация**
   - [ ] Расширить `docs/TESTS_SYSTEM_GUIDE.md`: структура e2e тестов, команды запуска, где искать артефакты.

> 🧩 Эти задачи заметно повышают надёжность, но требуют поддержки инфраструктуры (эмуляторы, среды тестирования).

---

## 6. 🧹 Качество кода и архитектура

### 6.1 [P: M | E: M] Удалить устаревшие режимы и дублирующийся код
1. [x] `App.jsx` и все потребители теперь используют единый хук `src/hooks/usePeriods.ts`, а старый `src/lib/usePeriods.js` удалён.
2. [x] `AuthProvider` остался только как compatibility-обёртка над `useAuthStore`, так что устаревшая логика не расширяется, сама проверка `usePeriods` перенесена в новую реализацию.
3. [x] Удалены legacy-скрипты (`scripts/transform.ts`, `scripts/verifyContent.ts`, `scripts/reconcileContent.ts`, `scripts/check-embeds.mjs`) — проект теперь чист, `PROJECT_MAP.md` обновлён командой `npm run analyze` (см. `scripts/analyze-project.ts`).

### 6.2 [P: M | E: S] Централизовать обработку ошибок
1. [x] `ErrorBoundary` и `ErrorToast` добавлены на уровне `src/main.jsx`; глобальные обработчики `window.error`/`unhandledrejection` вызывают `reportAppError`.
2. [x] Утилита `src/lib/errorHandler.ts` собирает ошибки, логирует их и уведомляет пользователей через тост — теперь ключевые bloques используют `reportAppError`.
3. [x] `useNotes`, `useTimelineState`, `useAuthStore` используют `reportAppError` вместо `alert`/локальных console.log, и ошибки теперь собираются централизованно с единым UI `ErrorToast`.

### 6.3 [P: L | E: M] Метрики и мониторинг
1. [ ] Внедрить базовый logger/telemetry (Sentry, Firebase Analytics, PostHog — по выбору).  
2. [ ] Отслеживать критические события: ошибки авторизации, проваленные загрузки, неуспешные записи в Firestore.  
3. [ ] Добавить раздел «observability» в документацию (как читать алерты, где смотреть логи).

> ❗Добавление аналитики усложняет поддержку (нужно следить за конфигурацией и безопасности), но сильно помогает при регрессиях.

---

## 7. 📘 Документация и процессы

### 7.1 [P: M | E: S] Вести QA/Smoke-лог
- [x] Создан `docs/qa-smoke-log.md` с колонками (дата, сценарий, команда, результат, ответственный).
- [x] После `npm run build` и `npx lighthouse ...` результаты зафиксированы (см. таблицу) — добавляйте каждую команду/ручной сценарий (CRUD, экспорт, админ-флоу) и ссылку на PR/коммит в таблицу.
- [x] Ссылки на лог уже есть в `README.md`, `docs/ARCHITECTURE_GUIDELINES.md`, `docs/TimelineGuide.md` и `docs/PLANS_OVERVIEW.md`, поэтому новые участники сразу видят историю проверок.

> 📝 Требование отмечено во множестве документов; без лога Phase 6 QA не считается выполненным.

### 7.2 [P: M | E: S] Синхронизировать Timeline документацию
- [x] Сравнили `docs/TIMELINE_REFACTORING_PLAN.md` и `docs/TimelineGuide.md`: plan now points to audit backlog/REFARCH, а гайд описывает реальный layout и ссылки на актуальные материалы.
- [x] В `TimelineGuide.md` добавили раздел Feature-based структуры (компоненты, хуки, экспорты), ссылку на чеклист Phase 6 и уточнили, что гайд опирается на актуальные задачи из `docs/audit-backlog.md`.
- [x] Задокументировали свежие результаты `npm run build` (см. section `Phase 6 QA и метрики`) и оставили инструкцию по Lighthouse — пока не запускался, но чеклист ждёт логов.

### 7.3 [P: M | E: S] Поддерживать тестовую документацию и статику
- [ ] Актуализировать `docs/TESTS_SYSTEM_GUIDE.md`, чтобы он отражал текущую архитектуру (TestsPage, DynamicTest, Zustand store).  
- [ ] Добавить npm-скрипт `ts:prune` и инструкцию в README (`npx ts-prune` → как читать отчёт).  
- [ ] В README явно сослаться на `ARCHITECTURE_GUIDELINES.md` и `TESTS_SYSTEM_GUIDE.md`, требуя прочтения перед новыми задачами/рефакторингами.

---

_После завершения каждой задачи ставьте ✅, добавляйте ссылку на PR/коммит и краткое резюме/регрессии (если были)._
