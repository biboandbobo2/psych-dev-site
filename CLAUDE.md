# Psych Dev Site — Custom Instructions

> **ВАЖНО:** Всегда отвечай на русском языке, если пользователь явно не попросит использовать другой язык.

## О проекте

Образовательный ресурс по психологии развития с интерактивными инструментами (заметки, тесты, таймлайн жизни).

**Стек:** React 19 + TypeScript + Vite + Firebase + Zustand

## Критические правила

### 1. Логирование
❌ **ЗАПРЕЩЕНО** использовать `console.*` напрямую (блокируется pre-commit хуком)
✅ **ВСЕГДА** используй `import { debugLog, debugError, debugWarn } from '@/lib/debug'`

**Исключения:** см. `ALLOWED` список в `scripts/check-console.cjs`

### 2. Code Splitting
⚠️ **НЕ импортируй статически** тяжёлые данные (> 500 KB):

```typescript
// ❌ ОПАСНО — добавит 4.7 MB в чанк
import { EVENT_ICON_DATA_URL_MAP } from '@/data/eventIconDataUrls';

// ✅ ПРАВИЛЬНО — dynamic import
const { EVENT_ICON_DATA_URL_MAP } = await import('@/data/eventIconDataUrls');
```

**Детали:** `vite.config.js` содержит настройки `manualChunks`. При добавлении тяжёлых страниц (> 200 KB) добавляй их в конфигурацию.

### 3. Lazy Loading
Все роуты лениво загружаются через `src/pages/lazy.ts`. При добавлении новой страницы:
1. Создай `React.lazy` import в `src/pages/lazy.ts`
2. Оберни в `<Suspense fallback={<PageLoader />}>`
3. Проверь размер чанка: `npm run build`

### 4. Архитектура
- **Barrel Exports** — все модули через `index.ts` (`src/components/`, `src/hooks/`, `src/utils/` и т.д.)
- **Модульность** — компоненты < 400 строк, функции < 50 строк
- **Custom Hooks** — переиспользуемая логика только в хуках

### 5. Cloud Functions — импорты
`functions/tsconfig.json` использует `"moduleResolution": "NodeNext"`. В Cloud Functions **все относительные импорты должны включать расширение `.js`**:

```typescript
// ❌ НЕПРАВИЛЬНО — сломает сборку
import { sendTelegram } from './lib/telegram';

// ✅ ПРАВИЛЬНО
import { sendTelegram } from './lib/telegram.js';
```

Это касается `functions/src/` и `shared/`. На клиенте (Vite) расширения не нужны.

## Проверки перед коммитом

```bash
# Быстрая валидация (рекомендуется)
npm run validate       # Всё ниже + build (~5-10 сек)

# Или по отдельности:
npm run lint           # ESLint
npm run check-console  # Проверка console.*
npm run check:init     # Module initialization
npm run build          # Production build
npm test               # Unit тесты

# Полная валидация (перед пушем)
npm run validate:full  # validate + tests (~15-30 сек)
```

**Автоматические проверки:**
- **Pre-commit hook:** `lint` + `check-console` + `check:init`
- **Pre-push hook:** `validate` (полная валидация)

См. подробности: `docs/development/testing-workflow.md`

## Деструктивные операции

⛔ **Перед любым деструктивным действием** (удаление функций, файлов, коллекций, force push и т.д.):
1. **Объясни** что именно будет удалено/перезаписано и почему
2. **Покажи контекст** — git-историю, зависимости, текущее использование
3. **Дождись явного одобрения** пользователя

Никогда не удаляй продакшн-ресурсы (Cloud Functions, Firestore-коллекции, Storage-файлы) без явного подтверждения.

## Рефакторинг — рабочий процесс

При многошаговом рефакторинге (3+ изменений):
1. **Составь план** — пронумерованный список улучшений
2. **Реализуй по одному** — каждое улучшение отдельно
3. **Тесты после каждого шага** — запусти `npm test` и убедись что зелёные
4. **Коммить инкрементально** — один коммит = одно завершённое улучшение
5. **Не оставляй незавершённое** — если сессия заканчивается, текущий шаг должен быть закоммичен или откачен

## Тестирование и деплой

**Основная ветка:** `main`

**Production URL:** https://psych-dev-site.vercel.app

⚠️ **ВАЖНО:** Каждый push в main автоматически деплоится на Vercel. Для тестирования создавай feature-ветки.

### Деплой Cloud Functions
Перед деплоем (`npm run firebase:deploy:all`):
1. Перечисли функции, которые будут добавлены, обновлены или удалены
2. Для удалений — покажи git-историю и обоснуй безопасность
3. Дождись одобрения перед выполнением

## Документация

📘 **Начни здесь:**
- `docs/QUICK_START.md` — быстрый старт (5 минут)
- `docs/README.md` — навигация по всей документации
- `docs/architecture/overview.md` — обзор архитектуры проекта

📘 **ОБЯЗАТЕЛЬНО читай перед изменениями:**
- `docs/architecture/guidelines.md` — архитектурные правила (SRP, DRY, структура)
- `docs/development/testing-workflow.md` — workflow разработки и тестирования
- `docs/processes/qa-smoke-log.md` — лог тестирования (фиксируй каждый запуск!)

📚 **Специфичная документация:**
- `docs/guides/testing-system.md` — система тестирования
- `docs/guides/timeline.md` — система Timeline
- `docs/guides/multi-course.md` — мультикурсовая система
- `docs/guides/book-rag.md` — Book RAG поиск
- `docs/guides/feedback-system.md` — система обратной связи (Telegram)
- `docs/processes/audit-backlog.md` — текущие задачи и приоритеты
- `README.md` — обзор проекта, маршруты, Firestore коллекции
- `CHANGELOG.md` — история изменений проекта

## Быстрые команды

```bash
# Разработка
npm run dev               # Dev сервер (port 5173)
npm run build             # Production build
npm run preview           # Preview build (port 4173)

# Валидация
npm run validate          # Быстрая проверка (~5-10 сек)
npm run validate:full     # Полная проверка с тестами (~15-30 сек)

# Тесты
npm test                  # Unit тесты (watch)
npm run test:e2e:prod     # E2E production smoke tests

# Firebase
npm run firebase:deploy:all  # Деплой (storage + rules + functions)
```

## Написание тестов

При написании тестов:
- **Сначала прочитай реальную реализацию** — не делай предположений о сигнатурах, возвращаемых типах или поведении хуков
- **Cloud Functions тесты** (`functions/src/**/*.test.ts`) — используй `.js` расширения в импортах (NodeNext resolution)
- **Клиентские тесты** (`src/**/*.test.ts`) — расширения в импортах не нужны (Vite)
- **Перед рефакторингом** — напиши тесты, покрывающие текущее поведение, убедись что зелёные, затем рефактори

## Ключевые файлы

- `vite.config.js` — **критично!** настройки `manualChunks` для code splitting
- `src/pages/lazy.ts` — централизованные lazy imports
- `src/lib/debug.ts` — утилиты логирования
- `scripts/check-console.cjs` — pre-commit проверка console.*
- `src/data/eventIconDataUrls.ts` — ⚠️ 4.7 MB, **только dynamic import!**
- `functions/tsconfig.json` — **NodeNext** module resolution (расширения `.js` обязательны)

## Оформление ответов
- В первом ответе выведи маркер: <!--claude:MEM v2025-11-14-->
- Перед большими правками, при планировании работы коротко перечисли архитектурные инварианты, на которые опираешься (из ARCHITECTURE_*)

---

**Последнее обновление:** 2026-02-05
